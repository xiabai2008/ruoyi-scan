# D16 Grafana 数据源自动配置
# 启动时自动注入 Prometheus 数据源，免手动配置
apiVersion: 1

datasources:
  - name: Prometheus
    type: prometheus
    access: proxy
    url: http://prometheus:9090
    isDefault: true
    editable: false
    jsonData:
      timeInterval: '15s'
