# Ruoyi-Scan 开发方案（Trae 落地版）

> **⚠️ 2026-07-18 状态说明**：本文件为项目初期"插件化重构"方案，阶段一已全部完成。
> 项目已于 2026-07-18 转向"专注若依做深"路线，当前有效规划见 `P0P1P2升级计划.md`。
> 本文件保留作历史参考，其中"阶段二通用 CMS 框架"预览已废弃（thinkphp/weaver/shiro/struts2 抽离至 `../cms-scan-extras/`）。

---

> 版本：v1.0　|　日期：2026-07-16　|　适用工具：Trae AI IDE
> 配套约束文件：`agents.md`（Trae 编码时强制加载，锁定代码风格）
> 战略共识：**先把若依专用做精（阶段一），再横向升级为「指纹识别 + 插件路由」的通用 CMS 框架（阶段二）**。

---

## 一、方案总览

### 1.1 当前状态评估

现有 `Ruoyi-Scan.py`（单文件单类 `RUOYI_SCAN`）已具备三块能力，基础扎实：

| 能力 | 现状 | 评价 |
|------|------|------|
| 目录扫描 | `path_scan()` + `ruoyi.txt`（695 条端点） | 字典质量高，覆盖 Actuator/Druid/Swagger |
| 漏洞检测 | 4 个内嵌 POC | 逻辑正确，但写死在函数中、无法扩展 |
| 弱口令爆破 | `web_login()` + `password.txt` | 仅覆盖 Druid，缺后台登录 |

**核心短板**：无插件化结构、无指纹识别、无报告输出、无并发/会话/代理、判定为二态（网络错误误判为「不存在」）。

### 1.2 目标形态（阶段一交付）

一个**插件化的若依专用扫描器**，具备：POC 插件热插拔、并发 + 限速、Cookie/代理会话、HTML/JSON/CSV 报告、三态判定降误报，并**预留指纹层与路由层接口**，为阶段二零成本演进铺路。

---

## 二、架构设计

### 2.1 分层架构

```
目标输入
  → [第1层] 侦察与指纹识别（阶段一 stub，阶段二实装）
  → [第2层] 插件路由引擎（阶段一硬编码 ruoyi）
  → [第3层] POC 插件包执行（ruoyi 插件集，并发）
  → [第4层] 结果汇总与报告（HTML/JSON/CSV）
```

### 2.2 目标目录结构

```
Ruoyi-Scan/
├── main.py                  # CLI 入口（-h/-u/-m/-p/-l 向后兼容 + 新长参数）
├── config/settings.py       # 全局配置：线程/限速/代理/超时/字典路径
├── core/
│   ├── models.py            # ScanResult / FingerprintResult
│   ├── engine.py            # ThreadPoolExecutor 并发 + 令牌桶限速 + 编排
│   ├── loader.py            # 插件动态发现（pkgutil 扫描 plugins/）
│   ├── session.py           # SessionManager：Cookie/代理/重试/keep-alive
│   ├── fingerprint.py       # Fingerprint 接口 + RuoyiFingerprint（stub）
│   ├── router.py            # Router：指纹→插件包（阶段一硬编码 ruoyi）
│   └── report.py            # ReportBuilder：HTML/JSON/CSV
├── plugins/
│   ├── base.py              # PluginBase 抽象基类
│   └── ruoyi/
│       ├── __init__.py      # plugin_list = [...]
│       ├── file_read.py         # 任意文件读取
│       ├── file_read_time.py    # 定时任务任意文件读取
│       ├── sql_inject_role.py   # SQL 报错注入(role)
│       ├── sql_inject_dept.py   # SQL 报错注入(dept)
│       ├── druid_brute.py       # Druid 弱口令爆破
│       ├── directory_scan.py    # 目录扫描（接管 path_scan）
│       └── （阶段一新增）file_upload.py / job_rce.py / thymeleaf_ssti.py / unauth_batch.py
├── lib/
│   ├── http.py              # join_url 归一化（双斜杠处理等）
│   ├── matcher.py           # 降误报判定工具（正/负向联合）
│   └── colors.py            # ANSI 颜色常量（沿用现有配色语义）
└── data/
    ├── ruoyi.txt            # 目录字典（原样迁移）
    └── password.txt         # 口令字典（原样迁移）
```

### 2.3 关键接口定义

**插件基类**（`plugins/base.py`）：
```python
class PluginBase(ABC):
    name: str; cve: str; severity: str        # high/medium/low
    category: str; description: str; fix: str
    def verify(self, target, session) -> ScanResult: ...
```

**结果模型**（`core/models.py`）：`ScanResult(kind, name, severity, status, url, evidence, extra)`，
其中 `status ∈ {CONFIRMED, SAFE, UNKNOWN}`（网络异常归 UNKNOWN，绝不判 SAFE）。

**指纹接口**（`core/fingerprint.py`，阶段一 stub）：
`Fingerprint.detect(target, session) -> FingerprintResult(cms, version, confidence, matched)`。

**路由**（`core/router.py`）：`mapping = {"ruoyi": "plugins.ruoyi.plugin_list"}`，阶段二加映射即可，引擎零改动。

---

## 三、实施路线（五步，建议顺序执行）

> 每一步交付后对照 `Ruoyi-Scan.py` 验证无功能回退，再进入下一步。

### 步骤 1 · 搭建插件化骨架
- 新建 `models / base / loader / engine / session / settings / main` + 1 个 demo 插件。
- **验收**：`python main.py -p http://example.com` 能加载并运行 demo 插件，输出格式符合现有风格。

### 步骤 2 · 无损迁移现有能力
- 将 4 个 POC + Druid 爆破 + 目录扫描逐一迁为 `plugins/ruoyi/*.py`。
- **严格保留**所有原判定表达式（见 `agents.md` §6 铁律）。
- **验收**：对同一靶场，新旧代码判定结果完全一致。

### 步骤 3 · 指纹层与路由层接口（预留）
- 实装 `fingerprint.py`(stub 返回 ruoyi) + `router.py`(硬编码) + `lib/fingerprint_features.py`(空实现)。
- **验收**：主流程走完整链路 目标→指纹→路由→插件→结果。

### 步骤 4 · 报告输出
- 实现 `report.py` 的 HTML（风险着色 + 修复建议）/ JSON（供 CI）/ CSV 三格式。
- **验收**：一次扫描产出结构化报告，UNKNOWN/SAFE 可见但不计入漏洞数。

### 步骤 5 · 补齐若依专项漏洞 + 工程化增强
- 新增插件：任意文件上传 `/common/upload`、定时任务 RCE `/monitor/job`、Thymeleaf/SpEL 注入、未授权访问批量检测（Actuator/Druid/Swagger/后台）、后台默认口令。
- 工程化：并发 + 限速、Cookie/代理、后台登录爆破（验证码/锁定策略处理）、CLI 长参数 `--proxy/--threads/--rate/--report`。
- **验收**：并发下扫描稳定，误报可控，报告完整。

---

## 四、Trae 使用指引

1. **加载约束**：将 `agents.md` 置于项目根目录，Trae 会自动作为项目规则加载；编码前确认 AI 已读取。
2. **按步驱动**：每次只让 Trae 完成路线中的**一个步骤**，产出后人工对照验收再继续，避免一次生成大量未验证代码。
3. **风格校验**：每步完成后用 `agents.md` §9 检查清单自查（配色、前缀、注释、命名、无损迁移、接口预留）。
4. **靶场验证**：建议本地搭建若依靶场（如 RuoYi v4.x）做回归对照，确保迁移无功能回退。
5. **提交规范**：一个插件/模块一个提交，中文提交信息 `类型: 简述`。

---

## 五、风险与对策

| 风险 | 对策 |
|------|------|
| 重构导致原有判定回退 | 步骤 2 强制逐项对照原脚本，铁律清单核对 |
| 指纹误判（阶段二命门） | 多特征交叉判定（favicon hash + 特征路径 + 响应头），阶段一先留接口 |
| 并发触发目标 WAF/封禁 | 令牌桶限速 + 可配置线程数 + 代理支持 |
| 误报影响可信度 | 三态判定 + `matcher` 正负向联合 + 注入差分 |
| 风格漂移 | `agents.md` 硬约束 + 每步风格自查清单 |

---

## 六、阶段二预览（仅铺垫，暂不实施）

填充 `fingerprint_features.py` 真实特征 → 新增 `plugins/thinkphp/` 等 CMS 插件包并在 `Router.mapping` 注册 → **引擎与报告零改动**即支持多 CMS 自动识别与定向检测。届时若依模块自然成为「插件 #1」。

---

## 关键交付物清单
- 约束文件：`agents.md`（已交付）
- 本方案：`开发方案.md`（已交付）
- 参考保留：`Ruoyi-Scan.py`（legacy，重构验证通过前不删除）
- 字典：`ruoyi.txt` / `password.txt`（原样迁移至 `data/`）
