# Ruoyi-Scan 依赖（阶段七 Docker 部署）
# scanner 主程序仅依赖 requests；flask 供 lab 靶场；requests-mock 供 tests
# 注：pyyaml（Nuclei 兼容用）已随 plugins/nuclei 迁移至 cms-scan-extras/

# === 主依赖（零系统依赖，必装）===
requests>=2.28
flask>=2.3
requests-mock>=1.11

# === D8 报告增强（可选，缺时自动降级为 HTML/JSON/CSV）===
# PDF 报告（reportlab 内置 STSong-Light CJK 字体，零系统字体依赖）
reportlab>=4.0
# Word 报告（python-docx 数据驱动，无需模板文件）
python-docx>=1.1
# Excel 报告（openpyxl 可读+写，原生图表+条件格式）
openpyxl>=3.1

# === D9 Web API（可选，仅 --serve 模式需要）===
# FastAPI + uvicorn 提供 REST API + WebSocket + Web 控制台
fastapi>=0.100
uvicorn[standard]>=0.23
