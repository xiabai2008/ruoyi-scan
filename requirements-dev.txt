# 开发与测试依赖（CI 用）
# 安装：pip install -r requirements-dev.txt
-r requirements.txt

# 测试框架
pytest>=7.4
pytest-timeout>=2.2
pytest-cov>=4.1
pytest-asyncio>=0.21

# FastAPI TestClient 依赖（starlette.testclient 需要）
httpx2>=2.7.0
