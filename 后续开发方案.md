# Ruoyi-Scan 后续升级开发方案

> **⚠️ 2026-07-18 废弃说明**：本文件为早期"多 CMS 扩充"规划，已随项目转向"专注若依做深"路线而废弃。
> 当前有效规划见 `P0P1P2升级计划.md`（做深若依 D1-D5 阶段）与 `trae_prompt.md`。
> 本文件保留仅作历史参考，其中阶段四泛微 OA、阶段八 ThinkPHP 扩充等内容对应的插件已抽离至 `../cms-scan-extras/`。

---

> 当前基线：3 CMS（RuoYi / ThinkPHP / Spring Boot）/ 30 POC / 批量扫描 -f / HTML+JSON+CSV 报告
> 框架零改动原则已验证：新增 CMS 仅加特征库数据 + 插件包 + 靶场 + 回归，引擎/路由/报告自动适配。

---

## 阶段四：第四个 CMS — 泛微 e-cology OA（优先级最高）

### 背景
泛微 e-cology 是国内政企最常见 OA 系统之一，历史漏洞密集，实战覆盖面广，且与现有 3 个 CMS（均为框架型）攻击面完全不同（OA 业务逻辑型）。

### 指纹特征
- 强特征路径：`/` 主页含 `e-cology` 或 `泛微` 标题，`/login/Login.jsp` 登录页
- 弱特征：响应头 `Set-Cookie: ecology_JSessionid`、`/weaver/` 路径前缀
- favicon：泛微默认 favicon（需采集真实实例 hash）

### 计划 POC（5-6 个，覆盖不同攻击面）
| 序号 | 插件 | 漏洞类型 | CVE/CNVD | 危害 |
|------|------|----------|-----------|------|
| 1 | `weaver_unauth.py` | 未授权访问 / 信息泄露 | — | medium |
| 2 | `weaver_file_upload.py` | 任意文件上传 getshell | CNVD-2021-49104 | high |
| 3 | `weaver_sqli.py` | SQL 注入 | CNVD-2022-43245 | high |
| 4 | `weaver_bsh_rce.py` | Beanshell RCE | — | high |
| 5 | `weaver_xml_rce.py` | XMLDecoder 反序列化 RCE | CVE-2022-26134 | high |
| 6 | `weaver_info_leak.py` | 配置文件泄霩 | — | medium |

### 交付物（严格按"新增 CMS 插件包标准流程"六步）
1. `lib/fingerprint_features.py` → 新增 `weaver` 条目
2. `plugins/weaver/` → `__init__.py` + 6 POC 插件
3. `core/router.py` → `'weaver': 'plugins.weaver'`
4. `lab/weaver_server.py` → Flask 双模式靶场
5. `tests/regression_weaver.py` → 12 例（6 × 2）+ `test_fingerprint.py` +2 例
6. 验证：pytest + regression + 端到端 vuln 全 CONFIRMED / safe 零误报

### 预计新增
- 代码文件：10 个（6 插件 + __init__ + 靶场 + 回归 + 2 改）
- POC 总数：30 → 36
- CMS 数：3 → 4

---

## 阶段五：指纹去重优化

### 问题
`core/fingerprint.py` 的 `detect_cms()` 为每个注册 CMS 独立发送主页/favicon 请求，多 CMS 时重复 GET 相同 URL，浪费请求量与扫描耗时。当前 3 CMS 至少 6 次重复请求（3 次主页 + 3 次 favicon）。

### 方案
```python
# 新增 core/cache.py — 单次扫描内的请求级缓存
class FingerprintCache:
    def __init__(self, session):
        self._cache = {}
        self._session = session
    
    def get(self, url):
        if url not in self._cache:
            self._cache[url] = self._session.get(url)
        return self._cache[url]
```
`detect_cms()` 使用 `FingerprintCache` 共享相同 URL 的请求响应，各 CMS 特征判定逻辑不变。

### 交付物
- `core/cache.py`：`FingerprintCache` 类
- `core/fingerprint.py`：`detect_cms()` 集成缓存
- `tests/test_cache.py`：缓存命中/未命中测试

### 预期收益
- 请求量降约 50%（3 CMS 从 ~9 次主页/favicon 降至 ~3 次）
- 扫描耗时降约 30%（网络往返减少）

---

## 阶段六：报告可视化加强

### 问题
当前 HTML 报告仅有表格，缺少直观的图表展示（风险分布饼图、CMS 漏洞对比柱状图等）。

### 方案
在 `core/report.py` 的 HTML 模板中嵌入 SVG 图表（纯标准库，零外部依赖）：
- **风险分布环形图**：high/medium/low 三段色环
- **批量汇总柱状图**：各目标漏洞数对比
- **CMS 漏洞分类饼图**：RCE vs 信息泄露 vs SQLi

### 交付物
- `core/report.py`：`to_html()` 模板增加 SVG 图表区块
- 图表数据源：`risk_distribution()` 已有，直接复用

---

## 阶段七：Docker 一键部署

### 问题
当前扫描器与靶场均需手动配置 Python 环境、启动多个进程，不便于演示与交付。

### 方案
```dockerfile
# Dockerfile — 扫描器镜像
FROM python:3.11-slim
COPY . /app
RUN pip install -r requirements.txt
ENTRYPOINT ["python", "main.py"]
```
```yaml
# docker-compose.yml — 扫描器 + 靶场全栈
services:
  scanner:
    build: .
    volumes: ["./reports:/app/reports"]
    command: -f targets.txt -p --report /app/reports
  
  lab-ruoyi:
    build: ./lab
    environment: [LAB_MODE=safe, LAB_PORT=8080]
  
  lab-thinkphp:
    build: ./lab
    environment: [LAB_MODE=vuln, LAB_PORT=8090]
```

### 交付物
- `Dockerfile`（扫描器）
- `docker-compose.yml`（扫描器 + 三个靶场）
- `lab/Dockerfile`（通用靶场镜像，ENTRYPOINT 由环境变量选择 server 文件）
- `requirements.txt`（已有）

---

## 阶段八：各 CMS 深度扩充

### RuoYi（当前 11 插件）
- RuoYi-Cloud（微服务版）Nacos 未授权 / Sentinel 未授权
- RuoYi 定时任务 SQL 注入变体
- RuoYi 文件下载路径穿越

### ThinkPHP（当前 10 插件）
- ThinkPHP 5.0.x Request 注入其他变体
- ThinkPHP 5.1.x 路由调度其他链
- ThinkPHP 3.x 旧版 RCE（历史遗留）

### Spring Boot（当前 9 插件）
- Spring Boot Actuator Jolokia MBean 其他链（JNDI/MLet 等）
- Spring Cloud Config 路径穿越
- Spring Boot 敏感端点 `/trace` `/dump`（旧版）
- Spring Boot Admin 未授权

---

## 阶段九：真实目标交叉验证

### 目的
当前全量验证基于本项目签名靶场（Tier A），需对真实公开漏洞靶机做交叉验证以提升实战可信度。

### 方案
- RuoYi：已有真实 4.7.8 实例 127.0.0.1:8080（已验证 SAFE + VULN 双态）
- ThinkPHP：Apache/Apache + PHP 手动部署 v5.0.23 或使用 vulhub ThinkPHP 靶场
- Spring Boot：vulhub 或公开 Spring Boot 靶场镜像

### 交付物
- `lab/REAL-*.md`：各 CMS 真实靶场验证文档（环境搭建 + 对拍结果）
- 若发现漏报/误报 → 修复对应插件

---

## 时间线建议

| 阶段 | 内容 | 工作量 |
|------|------|--------|
| 四 | 泛微 OA 插件包 | ★★★ 中等（6 个新 POC + 靶场） |
| 五 | 指纹去重 | ★☆☆ 小（1 个新模块 + 改指纹模块） |
| 六 | 报告图表 | ★★☆ 中（SVG 图表嵌入 HTML） |
| 七 | Docker 部署 | ★☆☆ 小（Dockerfile/compose） |
| 八 | 各 CMS 深度扩充 | ★★★ 中（多轮迭代） |
| 九 | 真实目标验证 | ★★☆ 中（需真实靶机环境） |

---

## 架构红线（任何阶段不得违反）

1. **零改引擎/路由/报告原则**：新增 CMS 或漏洞仅加数据/插件，`core/engine.py`、`core/router.py`（仅 mapping 加一行）、`core/report.py` 不改动主体逻辑。
2. **判定三态**：CONFIRMED / SAFE / UNKNOWN，网络异常绝不判 SAFE。
3. **插件接口**：继承 `PluginBase`，`verify(target, session) -> ScanResult`。
4. **URL 拼接**：`lib.http.join_url(target, '/path')`，path 用前导斜杠。
5. **验证闭环**：新增任何功能 → pytest 全绿 + regression 全绿 + 端到端对拍双态通过。
