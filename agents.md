# AGENTS.md — Ruoyi-Scan 项目 AI 编码约束

> 本文件是 Trae / AI Agent 在本仓库编写代码时**必须遵守的硬约束**。
> 目标：在插件化重构与后续扩展中，**严格延续现有代码的视觉风格、输出格式与工程习惯**，保证新老代码浑然一体。
> 优先级：本文件 > 个人偏好 > 通用最佳实践。如与通用规范冲突，以本文件为准。

---

## 1. 项目定位

- **名称**：Ruoyi-Scan —— 针对若依(Ruoyi)系统框架的综合漏洞扫描工具。
- **当前版本**：`1.0.0`（作者：雪山乘客）。
- **技术栈**：Python 3 + `requests`，命令行(CLI)工具，控制台彩色输出。
- **演进路线**：阶段一「若依专用做精 + 插件化重构」→ 阶段二「指纹识别 + 插件路由的通用 CMS 框架」。阶段一所有代码**必须预留**指纹层与路由层接口。

---

## 2. 语言与依赖约束

- **Python 版本**：3.8+。语法保持简洁，避免使用过新特性导致低版本不可运行。
- **第三方依赖**：默认仅 `requests`。新增依赖必须先在开发方案中说明理由，能用标准库解决的不引入第三方库。
- **禁止**：引入重型框架（如 scrapy）、GUI 库；禁止联网下载运行外部代码。
- **编码声明**：所有 `.py` 文件统一 UTF-8；读取字典文件统一 `encoding='utf-8'`。

---

## 3. 输出风格规范（**最高优先级，逐字对齐现有代码**）

### 3.1 ANSI 颜色调色板（唯一标准）

现有代码使用如下 ANSI 转义色，**新增代码必须复用同一套语义，不得自创配色**：

| 颜色码 | 含义（语义） | 使用场景 |
|--------|-------------|----------|
| `\033[32m` 绿色 | 正向 / 命中 / 成功 / 存在 | 存在漏洞、状态码 2xx、标题非空、爆破成功 |
| `\033[31m` 红色 | 负向 / 未命中 / 失败 / 不存在 | 不存在漏洞、非 2xx 状态码、标题 `NULL`、模式=综合扫描高亮 |
| `\033[33m` 黄色 | 过程提示 / 字段标签 / 分隔语境 | `[*]` 提示行、字段名（响应/标题/长度）、当前扫描模式标签 |
| `\033[0m` 重置 | 结束着色 | 每段彩色输出**必须**以 `\033[0m` 收尾 |

> 落地建议：在 `lib/` 下新建 `colors.py` 定义常量（`GREEN='\033[32m'` 等），**但输出的视觉效果必须与现有一致**。禁止改变已有语义映射。

### 3.2 消息前缀约定（严格保留）

- `[*]` —— 信息提示 / 正向结果条目（如 `[*]存在任意文件读取漏洞`）。
- `[/]` —— 负向结果条目（如 `[/]不存在任意文件读取漏洞`）。
- 分隔线 —— **89 个 `-` 字符**（对应现有 `'-'*89` 视觉宽度），用于模块之间分隔。
- 关键结论用整行彩色包裹，例如：`\033[32m[*]存在定时任务任意文件读取漏洞\033[0m`。

### 3.3 Banner 规范

- 保留现有 ASCII Art（RUOYI SCAN 字样）。
- Banner 尾部固定包含：版本号、`By.<作者>`、`Github:`、`联系方式:`，配色沿用 绿(art) + 黄(信息) 组合。
- 版本号变更时同步更新 Banner 与文档。

### 3.4 已知风格不一致（新代码需修正为标准）

- 现有 `web_login()` 中「登录失败」误用了绿色 `\033[32m`。**新代码统一按 3.1 语义**：成功=绿、失败=红。迁移旧代码时保留其判定逻辑，但可将失败着色规范为红色（属视觉修正，非逻辑变更）。

---

## 4. 代码结构与命名规范

### 4.1 目录结构（阶段一目标，新增文件须归位）

```
Ruoyi-Scan/
├── main.py                  # CLI 入口，参数分发（-h/-u/-m/-p/-l 必须向后兼容）
├── config/settings.py       # 全局配置：线程数/限速/代理/字典路径/超时
├── core/
│   ├── models.py            # ScanResult / FingerprintResult 数据模型
│   ├── engine.py            # 并发编排 + 限速
│   ├── loader.py            # 插件动态发现与加载
│   ├── session.py           # 会话封装：Cookie/代理/重试/keep-alive
│   ├── fingerprint.py       # 指纹接口 + RuoyiFingerprint（阶段一 stub）
│   ├── router.py            # 指纹→插件包路由（阶段一硬编码 ruoyi）
│   └── report.py            # HTML/JSON/CSV 报告渲染
├── plugins/
│   ├── base.py              # PluginBase 抽象基类
│   └── ruoyi/               # 若依插件包（每漏洞一文件）
├── lib/
│   ├── http.py              # URL 归一化等 HTTP 工具
│   ├── matcher.py           # 降误报判定工具
│   └── colors.py            # ANSI 颜色常量（见 3.1）
└── data/
    ├── ruoyi.txt            # 目录字典（原样迁移）
    └── password.txt         # 口令字典（原样迁移）
```

### 4.2 命名与注释

- **方法/函数/变量**：`snake_case`（延续现有 `path_scan` / `poc_scan` / `web_login` / `file_read` 习惯）。
- **类**：现有为全大写 `RUOYI_SCAN`；新增类统一用 `PascalCase`（如 `ScanEngine`、`PluginBase`）。保留旧类名不强制改名，避免破坏兼容。
- **注释**：**使用简体中文**，延续现有 `# 目录扫描` / `# 任意文件读取` 风格，每个 POC / 模块顶部一行中文说明其用途。
- **分节注释**：模块内功能块之间可用现有的长横线注释分隔风格。

### 4.3 CLI 兼容性（不可破坏）

- 现有参数语义必须保留：`-h` 帮助、`-u` 综合扫描、`-m` 目录扫描、`-p` 漏洞检测、`-l` 登录爆破。
- 新增能力用长参数补充（如 `--proxy` / `--threads` / `--rate` / `--report`），不得改变旧短参数含义。
- 保留结尾 `input('[*]工作完毕,感谢你的使用,回车退出.../')` 的交互习惯（可配置化，但默认保留）。

---

## 5. 插件开发规范（阶段一核心）

- 每个漏洞 = `plugins/ruoyi/` 下一个独立文件，一个插件类，继承 `PluginBase`。
- 插件必备元信息：`name`（中文漏洞名）、`cve`、`severity`（`high`/`medium`/`low`）、`category`、`description`、`fix`（修复建议）。
- 核心方法：`verify(self, target, session) -> ScanResult`。
- **判定三态化**：`CONFIRMED`（确认存在）/ `SAFE`（确认不存在）/ `UNKNOWN`（网络异常等无法判定）。**网络错误绝不能判为 SAFE**。
- **降误报**：多条件联合（状态码 + 正向关键字 + 负向排除 WAF/错误页）；注入类用「基准请求 vs payload 请求」差分。

---

## 6. 无损迁移铁律（重构时严禁功能回退）

将现有 `Ruoyi-Scan.py` 拆分为插件时，**逐项 1:1 迁移判定逻辑**，任何「原判存在、新判不存在」均视为回归缺陷：

| 能力 | 必须保留的关键点 |
|------|-----------------|
| 任意文件读取 | 原 URL `/common/download/resource?resource=...etc/passwd` + 判定 `'root' in t and ':/' in t` |
| 定时任务文件读取 | **严格保留** 固定 `JSESSIONID` + `edit → run → GET 2.txt` 的请求时序 |
| SQL 报错注入 ×2 | 原 `extractvalue(...)` payload、headers、判定 `'运行时异常' or 'database()'` |
| Druid 弱口令爆破 | 6 个用户名清单 + `/druid/submitLogin` + 判定 `'success' in t` |
| 目录扫描 | 读 `ruoyi.txt`，双斜杠归一化逻辑，状态码/标题/长度输出格式 |
| 字典文件 | `ruoyi.txt` 保留 `%20` 前缀条目；`password.txt` **保留空行口令，勿 strip** |

---

## 7. 安全与合规

- 本工具仅用于**授权范围内**的安全测试与学习研究。
- 代码中不得硬编码真实目标、真实凭据；示例一律用占位符或 `example.com`。
- 不得加入破坏性 payload（如删库、勒索、持久化后门）。
- 涉及利用（RCE/文件写入）的插件默认仅做**存在性验证**，不做实际破坏。

---

## 8. 提交与协作

- 提交信息使用简体中文，格式：`类型: 简述`（如 `feat: 新增任意文件上传插件`、`refactor: 目录扫描模块化`、`fix: 修正爆破失败着色`）。
- 每次改动保持单一职责，一个插件/一个模块一个提交。
- 重构过程中**保留** `Ruoyi-Scan.py` 作为 legacy 参考文件，不删除，直至新架构对照验证通过。

---

## 9. AI Agent 工作检查清单（每次编码后自检）

- [ ] 彩色输出是否符合 §3 调色板与前缀语义？
- [ ] 是否复用了现有的 `[*]`/`[/]` 前缀与 89 字符分隔线？
- [ ] 注释是否为简体中文、方法名是否 snake_case？
- [ ] 新插件是否携带完整元信息并实现三态判定？
- [ ] 是否破坏了旧 CLI 参数或旧判定逻辑（无损迁移铁律）？
- [ ] 是否预留了指纹层 / 路由层接口（不写死、可扩展）？
- [ ] 是否引入了未经批准的第三方依赖？
