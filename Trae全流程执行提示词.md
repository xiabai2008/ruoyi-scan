# Trae 全流程一次性执行提示词

> 用法：将本文件全文复制粘贴到 Trae 的 AI 对话框，让 Trae 依据本项目的 `agents.md` 与 `开发方案.md` 一次性完成「阶段一」全部开发。
> 已内置分步自检、分段提交与无损迁移回归，最大化降低「一次性跑完」带来的风格漂移与功能回退风险。

---

## 一、你的角色与目标

你是本项目（若依 / Ruoyi 综合漏洞扫描工具）的首席开发 AI。请严格依据项目根目录下的两份规范完成「阶段一」重构：

- `agents.md` —— 代码风格与硬约束（**必须逐条遵守，优先级最高**）
- `开发方案.md` —— 五步实施路线、目标目录结构与阶段二预览

目标：把现有单文件 `Ruoyi-Scan.py` 重构成插件化结构，**功能零回退**，并补齐若依专项能力。
**禁止**新增与本规范无关的「炫技」功能；**禁止**为跑通而简化现有判定逻辑。

---

## 二、执行前必做（不许跳过）

1. 完整读取 `agents.md` 与 `开发方案.md`，并**复述**你将遵守的 3 条最关键风格约束：
   - ANSI 配色语义映射（绿=命中/正向，红=失败/负向，黄=提示/标签，`\033[0m` 收尾）
   - 输出前缀（`[*]` 正向 / `[/]` 负向 / 89 字符分隔线 / banner 规范）
   - 命名与注释（snake_case + 简体中文注释；CLI `-h/-u/-m/-p/-l` 向后兼容）
2. 读取现有 `Ruoyi-Scan.py`、`ruoyi.txt`、`password.txt`，确保理解现有 4 个 POC、Druid 爆破、目录扫描字典的实现细节与判定逻辑。
3. 若当前目录未初始化 git，先执行 `git init` 并建立首个基线提交（保留原始单文件脚本）。

---

## 三、执行纪律（硬性，违反即不合格）

- 按《开发方案.md》第五步的**五步顺序**执行，严禁跳步，严禁并行大规模改写多文件（避免错误无法定位）。
- **每完成一步，必须先通过该步验收命令，再进入下一步。** 验收不过不得往下写。
- 每步通过验收后，立即 `git add -A && git commit -m "step N: <一句话说明>"` 分段提交，保证任意一步可独立回退。
- 现有 `web_login()` 登录失败误用绿色的瑕疵，在迁移时按 `agents.md` 要求修正为红色。

---

## 四、五步路线与每步验收

**Step 1 — 插件化骨架**
- 建立 `core/`（引擎、加载器、会话）、`plugins/ruoyi/`、`lib/`、`data/`、`tests/`、`main.py`。
- 验收：`python main.py -h` 不报错；banner 与分隔线符合 `agents.md`；导入 `core` 各模块无异常。

**Step 2 — 无损迁移现有能力**
- 将目录扫描、`poc_scan` 内 4 个 POC、Druid 爆破改写为插件 / 模块，行为与原脚本一致。
- 验收：见第五节回归测试全部通过；`python main.py -m <目标>`、`-p <目标>`、`-l <目标>` 与原脚本输出一致。

**Step 3 — 指纹识别 + 路由接口（预留）**
- 新增 `core/fingerprint.py`（多特征交叉判定：favicon hash + 特征路径 `/prod-api/`、验证码接口、登录页特征 + 响应头/标题）与 `core/router.py`（按指纹选择插件包，当前仅 ruoyi）。
- 验收：`python -c "import core.router, core.fingerprint"` 成功；指纹模块对已知若依特征返回 `cms=ruoyi` + 置信度。

**Step 4 — 报告输出**
- 结果对象渲染为 HTML / JSON / CSV，字段含：漏洞名称、URL、危害等级（高/中/低）、证据、修复建议；附扫描摘要（耗时、请求数、风险分布）。
- 验收：跑一次扫描后，三种格式文件均生成且字段完整、可读。

**Step 5 — 专项漏洞补齐 + 工程化增强**
- 新增若依专项 POC：后台任意文件上传 `/common/upload`、定时任务 RCE（`/monitor/job` 编辑 `invokeTarget`）、Thymeleaf/SpEL 模板注入、未授权访问批量检测（Actuator/Druid/Swagger/后台）、默认口令 `admin/admin123`。每个 POC 须明确「存在」判定规则以控误报。
- 工程化：并发扫描 + 限速、Cookie/会话管理、代理支持、日志/调试模式。
- 验收：新增 POC 各自有判定断言通过；并发与限速参数可配置且不影响判定准确性。

---

## 五、无损迁移回归验收（全部完成后必做，未过不许宣布完成）

编写 `tests/regression_ruoyi.py`，使用 `requests_mock`（或 `responses`）模拟若依响应，断言：

1. 任意文件读取 POC：命中含 `root:x:0:0` 的响应，**忽略**仅含 `root` 的噪声响应。
2. SQL 报错注入 POC：命中 `extractvalue` 报错的响应特征。
3. 定时任务读取链路（edit → run）：状态/响应判定正确。
4. Druid 爆破：对正确口令返回命中，错误口令返回未命中。
5. 目录扫描：对 200 / 403 状态码分类正确，标题提取正确。

> 若本地无可测若依靶场，**必须**用上述 mock 回归；不允许以「无靶场」为由跳过验收。

全部断言通过并执行 `git commit` 后，方可宣布完成。

---

## 六、完成交付物（最后输出）

完成后在对话中输出：
1. 变更文件清单（含新增 / 改写）。
2. 每一步的验收结论（命令 + 结果）。
3. 风险与未覆盖项清单（例如：后台登录验证码识别未做、未做真实靶场端到端验证）。
4. 运行示例命令（综合扫描、目录、漏洞、爆破四种模式）。

---

## 七、红线（禁止）

- 不得改变 `agents.md` 定义的 ANSI 配色语义。
- 不得新增 CLI 参数破坏 `-h/-u/-m/-p/-l` 向后兼容。
- 不得引入《开发方案.md》未规划的第三方重型依赖（优先标准库 + `requests`）。
- 不得删除或弱化现有 POC 的判定逻辑来「换取通过」。
