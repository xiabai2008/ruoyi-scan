# D10+ 路线图：Ruoyi-Scan 深化与扩展

> **创建时间**：2026-07-19
> **前置条件**：D1-D9 全部完成（380 测试通过，零回归）
> **目标**：在 D1-D9 基础上，补齐生产化、合规化、资产发现、反封禁、运维部署五大维度的能力短板

---

## 背景与现状

D1-D9 路线图交付了完整的若依专项漏洞扫描能力：16 个若依 POC + 14 个 Spring POC + 8 个通用 POC、3 条漏洞利用链、11 策略 WAF 绕过、6 格式报告、18 端点 Web API。但在端到端验收中发现若干可深化方向，集中在以下五个维度：

| 维度 | 现状短板 | 涉及方向 |
|------|----------|----------|
| 文档一致性 | `--portscan`/`--passive` 已实现但文档标"暂缓" | D10 |
| 生产化 | API 无鉴权、CORS 全开、任务全内存 | D11 |
| 合规化 | 16 个 POC 的 `cve` 字段全空、无 CVSS、无合规映射 | D12 |
| 反封禁 | 仅支持单 `--proxy`、无代理轮换 | D13 |
| 资产发现 | 无主动爬虫、无子域名、无 JS 端点提取 | D14 |
| 识别精度 | 指纹库仅 2 个 CMS、WAF 库仅 8 个 | D15 |
| 运维部署 | compose 无 API 服务、无监控 | D16 |

---

## 方向清单与优先级

| 方向 | 名称 | 工作量 | 优先级 | 依赖 |
|------|------|--------|--------|------|
| D10 | 文档与实现对齐 | 小 | 高 | 无 |
| D11 | API 鉴权 + 任务持久化 | 中 | 高 | 无 |
| D12 | CVE / CVSS / 合规映射 | 中 | 高 | 无 |
| D13 | 代理池 + 请求轮换 | 中 | 中 | 无 |
| D14 | 主动爬虫 + 子域名 + JS 提取 | 大 | 中 | 无 |
| D15 | 指纹库与 WAF 库扩充 | 小-中 | 中 | 无 |
| D16 | Docker Compose + 监控 | 中 | 中 | D11 |

**执行顺序**：D10 → D12 → D11 → D15 → D13 → D16 → D14

- D10 先做：零风险、立即收益、为后续清理认知
- D12 次做：独立于其他方向、直接提升报告价值
- D11 第三：让 `--serve` 生产可用，是 D16 的前置
- D15 第四：数据驱动、零侵入
- D13 第五：提升批量扫描实战能力
- D16 第六：依赖 D11 完成
- D14 最后：工作量最大、独立性最强

---

## 架构红线（全路线图通用）

1. **零改引擎/路由主体**：`core/engine.py` / `core/router.py` 不改主逻辑
2. **判定三态不变**：CONFIRMED / SAFE / UNKNOWN，网络异常绝不判 SAFE
3. **向后兼容**：所有新字段有默认值，旧代码不破坏；CLI 旧参数语义不变
4. **测试先行**：每个方向新增对应测试文件，全量回归零失败
5. **中文注释 + ANSI 配色**：延续 `agents.md` 规范
6. **可选依赖降级**：新增依赖（如 APScheduler、prometheus）缺失时自动降级，不阻断主流程

---

## D10 · 文档与实现对齐

**问题**：`--portscan` / `--passive` 在 `main.py` 已完整实现（调用 `core/portscan.py` 与 `core/proxy_server.py`），但 `P0P1P2升级计划.md` 废弃计划表中 P2-A/P2-B 仍标"⏸ 暂缓"；`README.md` 的 CLI 参数汇总行缺失 `--portscan` / `--passive` / `--passive-host` / `--passive-port`。

### 实施清单

| 序号 | 内容 | 文件 |
|------|------|------|
| 1 | P2-A 端口扫描状态从"⏸ 暂缓"改为"✅ 已完成" | `P0P1P2升级计划.md` |
| 2 | P2-B 被动扫描状态从"⏸ 暂缓"改为"✅ 已完成" | `P0P1P2升级计划.md` |
| 3 | CLI 参数汇总行补 `--portscan` / `--ports` / `--passive` / `--passive-host` / `--passive-port` | `README.md` |
| 4 | agents.md 演进路线补"端口扫描 + 被动代理已落地" | `agents.md` |

### 验收标准

- 文档中无"暂缓"状态与已实现功能冲突的描述
- README CLI 参数行覆盖 `main.py build_parser()` 中全部参数

---

## D11 · API 鉴权 + 任务持久化

**问题**：`api/app.py` 无任何鉴权中间件，任何人可 POST `/api/scan` 触发扫描；CORS 配置 `allow_origins=['*']` + `allow_credentials=True` 不安全；`core/task_registry.py` 全内存，进程重启任务历史与结果丢失。

### 实施清单

| 序号 | 内容 | 文件 |
|------|------|------|
| 1 | 新增 API Key 鉴权中间件（`X-API-Key` 头） | `api/auth.py`（新增） |
| 2 | 收紧 CORS（可配置 `allow_origins`，默认仅 localhost） | `api/app.py` |
| 3 | 新增 SQLite 持久层（标准库 `sqlite3`，零新增依赖） | `core/storage.py`（新增） |
| 4 | TaskRegistry 事件落盘 + 启动恢复 | `core/task_registry.py` |
| 5 | 配置项：`ApiAuth` + `Storage` | `config/settings.py` |
| 6 | `--serve` 支持 `--api-key` / `--cors-origins` 参数 | `main.py` |
| 7 | 测试：鉴权、CORS、持久化、重启恢复 | `tests/test_api_auth.py`（新增）、`tests/test_storage.py`（新增） |

### 技术方案

**鉴权**：API Key 模式（最简、无外部依赖）
- 启动时通过 `--api-key <key>` 或环境变量 `RUOYI_SCAN_API_KEY` 设置
- 未设置时默认仅允许 `127.0.0.1` 访问（开发模式）
- 中间件校验 `X-API-Key` 头，失败返回 401

**持久化**：SQLite（`data/tasks.db`）
- 表 `tasks`：task_id / status / target / created_at / finished_at / task_dict(JSON)
- 表 `events`：task_id / event_type / payload(JSON) / timestamp
- TaskRegistry 启动时从 SQLite 恢复未完成任务历史
- `notify()` 同步写盘（WAL 模式，不阻塞主流程）

### 验收标准

- 未带 `X-API-Key` 访问 `/api/scan` 返回 401
- CORS `allow_origins` 可配置，默认不含 `*`
- 进程重启后 `GET /api/scan` 仍能查询历史任务
- 新增测试全通过，既有 380 测试零回归

---

## D12 · CVE / CVSS / 合规映射

**问题**：`plugins/ruoyi/*.py` 所有 16 个 POC 的 `cve = ''` 全空；`core/models.py` 仅有 high/medium/low 三级，无 CVSS 分数；报告无合规映射（等保 2.0 / OWASP Top 10）。

### 实施清单

| 序号 | 内容 | 文件 |
|------|------|------|
| 1 | PluginBase 增 `cvss_vector` / `compliance` 字段 | `plugins/base.py` |
| 2 | ScanResult 增 `cve` / `cvss_score` / `compliance` 字段（向后兼容） | `core/models.py` |
| 3 | 16 个若依 POC 逐个补 `cve` / `cvss_vector` / 合规标签 | `plugins/ruoyi/*.py` |
| 4 | 14 个 Spring POC 同步补全 | `plugins/spring/*.py` |
| 5 | 8 个通用 POC 同步补全 | `plugins/common/*.py` |
| 6 | 报告渲染新字段（HTML/PDF/Word/Excel） | `core/report*.py` |
| 7 | 合规映射数据文件 | `data/compliance_map.yaml`（新增） |
| 8 | 测试：字段完整性 + 报告渲染 | `tests/test_compliance.py`（新增） |

### 合规映射标准

| 漏洞类型 | 等保 2.0 | OWASP Top 10 (2021) |
|----------|----------|---------------------|
| SQL 注入 | 8.1.3 | A03:2021 Injection |
| 任意文件读取 | 8.1.4 | A01:2021 Broken Access Control |
| 任意文件上传 | 8.1.4 | A04:2021 Insecure Design |
| RCE | 8.1.3 | A03:2021 Injection |
| 未授权访问 | 8.1.4 | A01:2021 Broken Access Control |
| SSTI | 8.1.3 | A03:2021 Injection |
| 默认口令 | 8.1.4 | A07:2021 ID & Auth Failures |

### 验收标准

- 所有 POC 的 `cve` 字段非空（无 CVE 的标注 `N/A` + 漏洞描述）
- 报告含 CVSS 分数 + 合规条款列
- 新增测试全通过，既有测试零回归

---

## D13 · 代理池 + 请求轮换

**问题**：`main.py` 仅支持单 `--proxy`；批量扫描 `-f targets.txt` 时单 IP 容易被目标/WAF 封禁；`lib/waf_bypass.py` 的 L4 源站直连只在 WAF 命中后触发，常规扫描时无 IP 轮换。

### 实施清单

| 序号 | 内容 | 文件 |
|------|------|------|
| 1 | 代理池管理（加载 + 健康检查 + 轮换 + 剔除） | `lib/proxy_pool.py`（新增） |
| 2 | SessionManager 接受 proxy_pool 而非固定 proxy | `core/session.py` |
| 3 | CLI 参数 `--proxy-file` / `--proxy-rotate` | `main.py` |
| 4 | 配置项 `ProxyPool` | `config/settings.py` |
| 5 | 测试 | `tests/test_proxy_pool.py`（新增） |

### 轮换策略

- **round-robin**（默认）：每次请求轮换到下一个代理
- **random**：随机选取
- **least-fail**：优先用失败率最低的代理
- 健康检查：连续 3 次失败自动剔除，每 5 分钟重试一次

### 验收标准

- `--proxy-file proxies.txt` 加载多代理轮换
- 单代理失败时自动切换到下一个
- 既有 `--proxy` 单代理模式完全兼容
- 新增测试全通过

---

## D14 · 主动爬虫 + 子域名 + JS 端点提取

**问题**：当前无主动爬虫，只有 `--passive` 被动代理捕获；`lib/origin_finder.py` 内部已有子域名枚举但仅服务于 WAF 绕过；无 JS 文件分析与端点提取。

### 实施清单

| 序号 | 内容 | 文件 |
|------|------|------|
| 1 | 轻量爬虫（BFS + 深度可配 + 同源限制） | `lib/crawler.py`（新增） |
| 2 | 子域名枚举（从 origin_finder 抽取并扩充，crt.sh + DNS） | `lib/subdomain.py`（新增） |
| 3 | JS 端点提取（正则提取 `/api/xxx` 路径） | `lib/js_extractor.py`（新增） |
| 4 | Orchestrator 在指纹识别后插入爬虫阶段 | `core/orchestrator.py` |
| 5 | CLI 参数 `--crawl` / `--crawl-depth` / `--subdomain` | `main.py` |
| 6 | 测试 | `tests/test_crawler.py`（新增）、`tests/test_js_extractor.py`（新增） |

### 技术方案

**爬虫**：标准库 `urllib` + 正则，零新增依赖
- BFS 队列，默认深度 2，最多 200 页
- 同源限制（不跨域抓取）
- 提取 `href` / `src` / `action` / `data-url` 属性
- 发现的路径自动加入扫描队列

**子域名**：crt.sh 证书透明度查询 + DNS 解析
- 复用 `lib/origin_finder.py` 的 crt.sh 查询逻辑
- 新增 DNS 暴力（基于 `data/subdomains.txt` 常见前缀）

**JS 提取**：正则匹配
- 下载页面引用的 `.js` 文件
- 正则提取 `/api/` / `/system/` / `/ruoyi/` 开头的路径
- 去重后加入扫描队列

### 验收标准

- `--crawl` 启用后自动发现页面并加入扫描
- `--subdomain` 启用后枚举子域名
- JS 提取的端点出现在扫描结果中
- 新增测试全通过

---

## D15 · 指纹库与 WAF 库扩充

**问题**：`lib/fingerprint_features.py` 仅注册 ruoyi/spring 两个 CMS；`lib/waf_features.py` 仅 8 个 WAF。识别不到的目标会回退默认 ruoyi 插件包。

### 实施清单

| 序号 | 内容 | 文件 |
|------|------|------|
| 1 | 补充若依 Cloud/微服务版特征 | `lib/fingerprint_features.py` |
| 2 | 补充易混框架负向特征（JeecgBoot/JFinal） | `lib/fingerprint_features.py` |
| 3 | WAF 库扩充（阿里云/腾讯云/AWS/F5/ModSecurity） | `lib/waf_features.py` |
| 4 | 测试 | `tests/test_fingerprint.py`（扩充） |

### 验收标准

- 若依 Cloud 版（RuoYi-Cloud）可识别
- 新增 WAF 类型可识别
- 既有指纹测试零回归

---

## D16 · Docker Compose + 监控

**问题**：`docker-compose.yml` 当前只有 scanner + 2 个签名靶场，无 `--serve` API 服务容器、无监控。

### 实施清单

| 序号 | 内容 | 文件 |
|------|------|------|
| 1 | 新增 `api` 服务容器（暴露 8000 端口） | `docker-compose.yml` |
| 2 | 新增 `/api/system/metrics` Prometheus 端点 | `api/metrics.py`（新增） |
| 3 | 可选 prometheus + grafana 监控栈 | `docker-compose.yml` |
| 4 | real-ruoyi 靶场容器化 | `docker-compose.yml` |
| 5 | 测试 | `tests/test_metrics.py`（新增） |

### 验收标准

- `docker compose up` 一键启动 API 服务 + 靶场
- `/api/system/metrics` 返回 Prometheus 格式指标
- 既有 docker-compose 用法兼容

---

## 测试总量预期

| 方向 | 新增测试 | 累计测试 |
|------|----------|----------|
| D1-D9（已完成） | — | 380 |
| D10 | 0（文档对齐） | 380 |
| D11 | 17 | 397 |
| D12 | 28 | 425 |
| D13 | 17 | 442 |
| D14 | 77 | 519 |
| D15 | 6 | 525 |
| D16 | 22 | 547 |
| D18+D24 | 44 | 591 |
| D19+D27 | 44 | 635 |
| D20+D21+D22+D26 | 55 | 690 |
| D23+D25+D28+D29 | 55 | 745 |
| D30+D31+D32+D33 | 65 | 810 |
| D34+D35+D36+D37 | 61 | 871 |

---

## 进度跟踪

| 方向 | 状态 | 完成时间 |
|------|------|----------|
| D10 | ✅ 已完成 | 2026-07-19 |
| D11 | ✅ 已完成 | 2026-07-19 |
| D12 | ✅ 已完成 | 2026-07-19 |
| D13 | ✅ 已完成 | 2026-07-19 |
| D14 | ✅ 已完成 | 2026-07-19 |
| D15 | ✅ 已完成 | 2026-07-19 |
| D16 | ✅ 已完成 | 2026-07-19 |
| D18 | ✅ 已完成 | 2026-07-19 |
| D19 | ✅ 已完成 | 2026-07-19 |
| D24 | ✅ 已完成 | 2026-07-19 |
| D27 | ✅ 已完成 | 2026-07-19 |
| D20 | ✅ 已完成 | 2026-07-19 |
| D21 | ✅ 已完成 | 2026-07-19 |
| D22 | ✅ 已完成 | 2026-07-19 |
| D26 | ✅ 已完成 | 2026-07-19 |
| D23 | ✅ 已完成 | 2026-07-19 |
| D25 | ✅ 已完成 | 2026-07-19 |
| D28 | ✅ 已完成 | 2026-07-19 |
| D29 | ✅ 已完成 | 2026-07-19 |
| D30 | ✅ 已完成 | 2026-07-19 |
| D31 | ✅ 已完成 | 2026-07-19 |
| D32 | ✅ 已完成 | 2026-07-19 |
| D33 | ✅ 已完成 | 2026-07-19 |
| D34 | ✅ 已完成 | 2026-07-19 |
| D35 | ✅ 已完成 | 2026-07-19 |
| D36 | ✅ 已完成 | 2026-07-19 |
| D37 | ✅ 已完成 | 2026-07-19 |

---

## D18 · 修复详情增强（fix_detail）

**问题**：38 个 POC 的 `fix` 字段仅一句话概述，缺乏可操作的修复代码、配置 diff、升级版本号、操作步骤；报告无"修复详情"列。

### 实施清单

| 序号 | 内容 | 文件 |
|------|------|------|
| 1 | PluginBase 增 `fix_detail` 类属性 + `_build_result()` 自动填充 | `plugins/base.py` |
| 2 | ScanResult 增 `fix_detail` 字段 + `to_dict()` | `core/models.py` |
| 3 | AggregatedVuln 增 `fix_detail` 继承 | `core/dedup.py` |
| 4 | 38 个 POC 填充具体修复代码（升级方案+代码修复+配置加固+WAF规则+合规映射） | `plugins/ruoyi/*.py`, `plugins/spring/*.py`, `plugins/common/*.py` |
| 5 | 报告 CSV/HTML/JSON 新增"修复详情"列 | `core/report.py` |
| 6 | 测试：字段完整性 + 报告渲染 + 38 POC 填充验证 | `tests/test_d18_d24_fix_reproduce.py` |

### fix_detail 内容规范

每个 POC 的 fix_detail 包含：
- 【升级方案】具体版本号 + pom.xml 配置
- 【代码修复】具体类名/方法名 + 代码 diff
- 【配置加固】application.yml / nginx.conf 配置
- 【WAF 规则】拦截规则
- 【合规】OWASP AXX:2021 + 等保 2.0 X.X.X

### 验收标准

- 38 个漏洞类 POC 的 fix_detail 全部非空（≥20 字符）
- 高危漏洞 fix_detail 含【升级方案】或【配置加固】
- 高/中危漏洞 fix_detail 含合规映射（OWASP/等保）
- 报告 CSV/HTML/JSON 含"修复详情"列
- 新增 44 测试全通过，既有测试零回归

---

## D24 · 漏洞复现命令（reproduce）

**问题**：扫描报告无复现步骤，安全人员需手动构造 PoC 命令验证漏洞。

### 实施清单

| 序号 | 内容 | 文件 |
|------|------|------|
| 1 | PluginBase 增 `reproduce` 类属性 + `_build_result()` 自动填充 | `plugins/base.py` |
| 2 | ScanResult 增 `reproduce` 字段 + `to_dict()` | `core/models.py` |
| 3 | AggregatedVuln 增 `reproduce` 继承 | `core/dedup.py` |
| 4 | 38 个 POC 填充复现命令（curl 探针 + 预期响应） | `plugins/ruoyi/*.py`, `plugins/spring/*.py`, `plugins/common/*.py` |
| 5 | 报告 CSV/HTML/JSON 新增"复现命令"列 | `core/report.py` |
| 6 | 测试：复现命令完整性 + 含 curl/python + 含预期响应 | `tests/test_d18_d24_fix_reproduce.py` |

### reproduce 内容规范

每个 POC 的 reproduce 包含：
- `# 1. 探测端点` → curl 命令
- `# 2. 利用 PoC` → curl 命令（含 payload）
- `# 预期响应` → 响应特征描述

### 验收标准

- 38 个漏洞类 POC 的 reproduce 全部非空
- reproduce 含 `curl` 或 `python` 命令
- reproduce 含预期响应说明（# 预期 / # 返回 / HTTP / 响应）
- 报告 CSV/HTML/JSON 含"复现命令"列
- 与 D18 共用测试文件，44 测试全通过

---

## D19 · 扫描模板（预设策略）

**问题**：用户每次扫描需手动组合多个 CLI 参数（--crawl --subdomain --js-extract --threads 等），缺乏预设策略。

### 实施清单

| 序号 | 内容 | 文件 |
|------|------|------|
| 1 | ScanTemplate 数据类（severity/category/compliance 过滤 + default_args） | `lib/scan_templates.py`（新增） |
| 2 | 4 个预置模板：quick/deep/compliance/dengbao | `lib/scan_templates.py` |
| 3 | apply_template() 填充未指定参数（CLI 优先） | `lib/scan_templates.py` |
| 4 | filter_plugins() 按模板规则过滤插件 | `lib/scan_templates.py` |
| 5 | CLI 参数 `--template` / `--template-list` | `main.py` |
| 6 | run_mode() 集成模板过滤 + 报告标签 | `main.py` |
| 7 | 测试：模板注册 + 应用 + 过滤 + 真实插件验证 | `tests/test_d19_d27_template_config.py` |

### 模板定义

| 模板 | 名称 | 过滤规则 | 默认参数 |
|------|------|----------|----------|
| quick | 快速扫描 | severity=high | threads=5, timeout=8 |
| deep | 深度扫描 | 无过滤 | crawl=True, subdomain=True, js_extract=True, crawl_depth=3 |
| compliance | OWASP 合规扫描 | category=vuln + compliance=OWASP | threads=3 |
| dengbao | 等保 2.0 合规扫描 | category=vuln + compliance=等保 | threads=3 |

### 验收标准

- `--template quick -u <target>` 仅跑高危插件
- `--template deep -u <target>` 自动启用爬虫+子域名+JS提取
- `--template compliance -u <target>` 仅跑含 OWASP 映射的插件
- `--template dengbao -u <target>` 仅跑含等保映射的插件
- CLI 显式参数优先于模板默认值
- 新增测试全通过

---

## D27 · YAML 配置文件

**问题**：复杂扫描参数需每次手动输入冗长命令行，无法保存和复用扫描配置。

### 实施清单

| 序号 | 内容 | 文件 |
|------|------|------|
| 1 | YAML 配置加载（PyYAML 优先 + 简易解析器回退） | `lib/config_loader.py`（新增） |
| 2 | 类型推断（bool/int/float/str） | `lib/config_loader.py` |
| 3 | key 别名映射（横线/下划线 → argparse dest） | `lib/config_loader.py` |
| 4 | merge_config_with_args() 合并优先级（CLI > 配置 > 默认） | `lib/config_loader.py` |
| 5 | create_example_config() 生成示例文件 | `lib/config_loader.py` |
| 6 | CLI 参数 `--config <path>` | `main.py` |
| 7 | main() 集成配置加载 + mode/target 映射 | `main.py` |
| 8 | 测试：加载 + 解析 + 合并 + 集成 | `tests/test_d19_d27_template_config.py` |

### 配置文件格式

```yaml
target: http://example.com/
mode: u
template: deep
proxy: http://127.0.0.1:8080
threads: 5
crawl: true
subdomain: true
```

### 优先级规则

1. CLI 显式参数（最高）
2. 配置文件参数
3. 模板默认参数
4. argparse 默认值（最低）

### 验收标准

- `--config scan.yml` 加载 YAML 配置
- CLI 参数优先于配置文件
- 配置文件中的 mode/target 映射到 -u/-m/-p/-l
- 无 PyYAML 时自动回退简易解析器
- 新增测试全通过

---

## D20 · 增量扫描与差异对比

**问题**：每次扫描都是全量，无法跟踪漏洞修复进度，无法对比两次扫描的差异。

### 实施清单

| 序号 | 内容 | 文件 |
|------|------|------|
| 1 | VulnFingerprint 漏洞指纹（name+url 去查询参数） | `lib/diff_scan.py`（新增） |
| 2 | DiffReport 差异报告（new/fixed/persisted/changed） | `lib/diff_scan.py` |
| 3 | diff_reports() 对比两个 JSON 报告 | `lib/diff_scan.py` |
| 4 | save_baseline() / load_report() 基线保存与加载 | `lib/diff_scan.py` |
| 5 | render_diff_report() 差异报告输出（JSON+HTML） | `lib/diff_scan.py` |
| 6 | CLI 参数 `--diff` / `--diff-only` / `--save-baseline` | `main.py` |
| 7 | run_diff_only_mode() 独立对比模式 | `main.py` |
| 8 | 测试：指纹 + 差异类型 + 渲染 + 集成 | `tests/test_d20_d21_d22_d26.py` |

### 差异类型

| 类型 | 含义 | 场景 |
|------|------|------|
| new | 新基线有，旧基线无 | 新引入的漏洞 |
| fixed | 旧基线有，新基线无 | 已修复的漏洞 |
| persisted | 两次都有，状态/严重度未变 | 仍未修复的漏洞 |
| changed | 两次都有，状态/严重度变化 | 漏洞状态改变 |

### 验收标准

- `--save-baseline` 保存本次扫描为基线
- `--diff <old.json>` 与历史报告对比，输出差异报告
- `--diff-only <old> <new>` 仅对比不扫描
- SAFE/UNKNOWN 不计入差异对比
- 差异报告含 JSON + HTML 双格式
- 新增 14 测试全通过

---

## D22 · SARIF 2.1.0 报告格式

**问题**：无 SARIF 格式输出，无法上传到 GitHub Code Scanning 或与其他安全工具集成。

### 实施清单

| 序号 | 内容 | 文件 |
|------|------|------|
| 1 | SARIF 2.1.0 规范实现（$schema/version/runs） | `core/report_sarif.py`（新增） |
| 2 | _build_rules() 从扫描结果构建规则定义 | `core/report_sarif.py` |
| 3 | _build_results() 构建 SARIF results 数组 | `core/report_sarif.py` |
| 4 | CVSS → GitHub Security Level 映射（error/warning/note） | `core/report_sarif.py` |
| 5 | to_sarif() / render_sarif() 输出函数 | `core/report_sarif.py` |
| 6 | render_all 集成 sarif 格式 | `core/report.py` |
| 7 | CLI `--report-format sarif` 支持 | `main.py` |
| 8 | 测试：版本 + 规则 + 结果 + level + 文件输出 | `tests/test_d20_d21_d22_d26.py` |

### SARIF 结构

```json
{
  "$schema": "https://docs.oasis-open.org/sarif/sarif/v2.1.0/cs01/schemas/sarif-schema-2.1.0.json",
  "version": "2.1.0",
  "runs": [{
    "tool": {"driver": {"name": "Ruoyi-Scan", "rules": [...]}},
    "results": [{"ruleId": "SQL注入", "level": "error", "locations": [...]}]
  }]
}
```

### 验收标准

- `--report-format sarif` 生成 `report.sarif`
- SARIF 版本为 2.1.0，含 $schema
- 仅 CONFIRMED 漏洞进入 results
- CVSS 9.0+ → error，4.0+ → warning，0.1+ → note
- rules 含 CVE/CVSS/合规映射属性
- `render_all(formats='all')` 生成 7 个文件（含 sarif）
- 新增 13 测试全通过

---

## D21 · 告警通知

**问题**：扫描完成后无自动通知，需人工查看报告。

### 实施清单

| 序号 | 内容 | 文件 |
|------|------|------|
| 1 | parse_notify_arg() 解析 --notify 参数 | `lib/notifier.py`（新增） |
| 2 | build_notification_message() 构建通知消息 | `lib/notifier.py` |
| 3 | _build_text_message() / _build_markdown_message() 双格式 | `lib/notifier.py` |
| 4 | send_webhook() 通用 Webhook | `lib/notifier.py` |
| 5 | send_dingtalk() / send_wechat() / send_feishu() 三大平台 | `lib/notifier.py` |
| 6 | send_email() 邮件通知（SMTP，HTML+纯文本） | `lib/notifier.py` |
| 7 | send_notifications() 多渠道分发 | `lib/notifier.py` |
| 8 | CLI 参数 `--notify`（可多次指定） | `main.py` |
| 9 | 测试：解析 + 消息构建 + mock 发送 | `tests/test_d20_d21_d22_d26.py` |

### 支持渠道

| 渠道 | 格式 | 说明 |
|------|------|------|
| webhook | `webhook=<url>` | 通用 Webhook（JSON payload） |
| dingtalk | `dingtalk=<url>` | 钉钉机器人（Markdown） |
| wechat | `wechat=<url>` | 企业微信机器人（Markdown） |
| feishu | `feishu=<url>` | 飞书机器人（Text） |
| email | `email=<addr>` | 邮件（SMTP 环境变量配置） |

### 验收标准

- `--notify webhook=<url>` 发送通用 Webhook
- `--notify email=<addr>` 发送邮件（需 SMTP 环境变量）
- 支持 `--notify` 多次指定多渠道
- 消息含目标/耗时/风险分布/漏洞列表（前 20 条）
- 超过 20 条漏洞自动截断
- 新增 15 测试全通过（含 mock 发送）

---

## D26 · 认证扫描增强

**问题**：无法扫描需认证才能访问的漏洞端点（如登录后的管理接口）。

### 实施清单

| 序号 | 内容 | 文件 |
|------|------|------|
| 1 | parse_auth_arg() 解析 --auth 参数 | `lib/auth_scan.py`（新增） |
| 2 | load_auth_file() 从文件加载认证信息 | `lib/auth_scan.py` |
| 3 | auto_login() 自动登录（表单/JSON） | `lib/auth_scan.py` |
| 4 | apply_auth_to_session() 应用到 SessionManager | `lib/auth_scan.py` |
| 5 | parse_login_arg() 解析 --auth-login | `lib/auth_scan.py` |
| 6 | CLI 参数 `--auth` / `--auth-file` / `--auth-login` | `main.py` |
| 7 | run_mode() 集成认证扫描 | `main.py` |
| 8 | 测试：解析 + 文件加载 + 应用 + 登录参数 | `tests/test_d20_d21_d22_d26.py` |

### 认证类型

| 类型 | 格式 | 说明 |
|------|------|------|
| cookie | `cookie="JSESSIONID=abc; token=xyz"` | Cookie 头注入 |
| bearer | `bearer=<jwt_token>` | Authorization: Bearer |
| header | `header="X-API-Key: value"` | 自定义头 |
| basic | `basic="user:password"` | Basic 认证 |
| login | `--auth-login admin:password` | 自动登录获取 Cookie/Token |

### 验收标准

- `--auth cookie="..."` 注入 Cookie 到所有请求
- `--auth bearer=<token>` 注入 Authorization 头
- `--auth-file cookies.txt` 从文件加载
- `--auth-login admin:pass` 自动登录
- 支持多种认证方式组合
- 新增 13 测试全通过

---

## D23 · 国际化（i18n，中英文报告）

**问题**：扫描报告仅支持中文，无法满足国际团队和英文环境用户需求。

### 实施清单

| 序号 | 内容 | 文件 |
|------|------|------|
| 1 | TRANSLATIONS 字典（zh/en 双语翻译键值） | `lib/i18n.py`（新增） |
| 2 | get_text() 翻译查询函数（带默认值回退） | `lib/i18n.py` |
| 3 | get_status_cn() / get_severity_cn() 状态/严重度翻译 | `lib/i18n.py` |
| 4 | get_csv_header() / get_html_title() 报告标题翻译 | `lib/i18n.py` |
| 5 | localize_report_dict() JSON 报告字典本地化 | `lib/i18n.py` |
| 6 | is_supported() 语言支持验证 | `lib/i18n.py` |
| 7 | CLI 参数 `--lang zh|en` | `main.py` |
| 8 | 测试：14 项翻译/本地化/回退验证 | `tests/test_d23_d25_d28_d29.py` |

### 支持语言

| 语言代码 | 语言 | 覆盖范围 |
|----------|------|----------|
| zh | 中文（默认） | 报告标题/状态/严重度/CSV 表头/HTML 标题 |
| en | English | 报告标题/状态/严重度/CSV 表头/HTML 标题 |

### 验收标准

- `--lang en` 生成英文报告标题和状态
- `--lang zh` 生成中文报告（默认）
- 未知语言回退到中文
- JSON 报告字典可本地化翻译
- 新增 14 测试全通过

---

## D25 · 插件 SDK（自定义插件开发框架）

**问题**：用户开发自定义插件需手动参考现有插件复制修改，缺乏标准化模板和验证工具。

### 实施清单

| 序号 | 内容 | 文件 |
|------|------|------|
| 1 | PLUGIN_TEMPLATE 插件代码模板（含全部必需字段） | `lib/plugin_sdk.py`（新增） |
| 2 | generate_plugin() 生成插件源代码 | `lib/plugin_sdk.py` |
| 3 | _to_pascal_case() / _to_filename() 名称转换工具 | `lib/plugin_sdk.py` |
| 4 | init_plugin_file() 生成插件文件到磁盘 | `lib/plugin_sdk.py` |
| 5 | check_plugin() 静态验证（正则检查必需字段/方法） | `lib/plugin_sdk.py` |
| 6 | check_plugin_by_import() 导入验证（实例化 + 属性检查） | `lib/plugin_sdk.py` |
| 7 | list_all_plugins() 枚举所有已加载插件元数据 | `lib/plugin_sdk.py` |
| 8 | generate_plugin_docs() 生成 Markdown 插件文档 | `lib/plugin_sdk.py` |
| 9 | CLI 参数 `--plugin-init` / `--plugin-check` / `--plugin-list` / `--category` | `main.py` |
| 10 | 测试：13 项模板生成/验证/枚举/文档测试 | `tests/test_d23_d25_d28_d29.py` |

### 插件开发工作流

```bash
# 1. 生成插件模板
python main.py --plugin-init my_rce_vuln --category ruoyi
# → 生成 plugins/ruoyi/my_rce_vuln.py

# 2. 编辑插件，实现检测逻辑（搜索 TODO 标记）

# 3. 验证插件完整性
python main.py --plugin-check plugins/ruoyi/my_rce_vuln.py
# → 检查必需字段/方法/继承关系

# 4. 列出所有插件
python main.py --plugin-list
# → 显示插件名称/类别/严重度/CVE/CVSS/合规
```

### 验收标准

- `--plugin-init <name>` 生成可导入的合法插件文件
- `--plugin-check <path>` 验证字段完整性 + 导入实例化
- `--plugin-list` 枚举所有插件元数据
- 生成的插件含 name/severity/category/description/fix/fix_detail/reproduce/compliance 字段
- 新增 13 测试全通过

---

## D28 · CI/CD 集成（GitHub Actions workflow）

**问题**：扫描工具无法集成到 CI/CD 流水线，无法在代码提交时自动扫描并按严重度阈值阻断流水线。

### 实施清单

| 序号 | 内容 | 文件 |
|------|------|------|
| 1 | 退出码定义（0=成功/1=发现漏洞/2=错误） | `lib/ci_runner.py`（新增） |
| 2 | should_fail_ci() 严重度阈值判断 | `lib/ci_runner.py` |
| 3 | get_ci_exit_code() CI 退出码计算 | `lib/ci_runner.py` |
| 4 | format_ci_summary() / format_ci_vulns() CI 日志格式化 | `lib/ci_runner.py` |
| 5 | GITHUB_ACTIONS_TEMPLATE GitHub Actions YAML 模板 | `lib/ci_runner.py` |
| 6 | GITLAB_CI_TEMPLATE GitLab CI YAML 模板 | `lib/ci_runner.py` |
| 7 | JENKINSFILE_TEMPLATE Jenkins Pipeline 模板 | `lib/ci_runner.py` |
| 8 | generate_ci_config() CI 配置文件生成 | `lib/ci_runner.py` |
| 9 | run_ci_mode() CI 模式入口 | `lib/ci_runner.py` |
| 10 | CLI 参数 `--ci` / `--severity-threshold` / `--ci-init` | `main.py` |
| 11 | run_mode() 集成 CI 退出码逻辑 | `main.py` |
| 12 | 测试：18 项阈值/退出码/格式化/模板/集成测试 | `tests/test_d23_d25_d28_d29.py` |

### CI 退出码

| 退出码 | 含义 | 触发条件 |
|--------|------|----------|
| 0 | 成功 | 无漏洞或漏洞低于阈值 |
| 1 | 发现漏洞 | 存在达到阈值的确认漏洞 |
| 2 | 错误 | 扫描过程出错 |

### 支持平台

| 平台 | 命令 | 输出 |
|------|------|------|
| GitHub Actions | `--ci-init github` | `.github/workflows/security-scan.yml` |
| GitLab CI | `--ci-init gitlab` | `.gitlab-ci.yml` |
| Jenkins | `--ci-init jenkins` | `Jenkinsfile` |

### 验收标准

- `--ci` 模式发现高危漏洞时退出码 1
- `--severity-threshold medium` 中危及以上触发 CI 失败
- `--ci-init github` 生成 GitHub Actions workflow 文件
- CI 日志含扫描摘要 + 漏洞列表
- 新增 18 测试全通过

---

## D29 · 漏洞知识库（离线 Wiki）

**问题**：扫描报告仅列出漏洞名称和修复建议，缺乏可离线浏览的漏洞知识库供安全团队参考。

### 实施清单

| 序号 | 内容 | 文件 |
|------|------|------|
| 1 | build_wiki_data() 从插件构建知识库统计数据 | `lib/vuln_wiki.py`（新增） |
| 2 | render_wiki_html() 生成 HTML Wiki（含搜索/过滤/统计卡片） | `lib/vuln_wiki.py` |
| 3 | render_wiki_json() 生成 JSON API 格式 | `lib/vuln_wiki.py` |
| 4 | generate_wiki() 知识库生成入口（支持多格式） | `lib/vuln_wiki.py` |
| 5 | CLI 参数 `--wiki` / `--wiki-output` | `main.py` |
| 6 | 测试：8 项数据构建/HTML 渲染/JSON/多格式/真实插件测试 | `tests/test_d23_d25_d28_d29.py` |

### Wiki 功能

| 功能 | 说明 |
|------|------|
| 搜索框 | 按漏洞名称/CVE/描述实时搜索 |
| 严重度过滤 | 高/中/低 三级过滤 |
| 合规过滤 | OWASP / 等保 2.0 合规标签过滤 |
| 统计卡片 | 总数/高危/中危/低危/含修复详情/含复现命令 |
| 漏洞卡片 | 名称/严重度/CVE/CVSS/合规/修复建议/修复详情/复现命令 |
| 响应式布局 | 自适应桌面/平板/手机 |

### 验收标准

- `--wiki` 生成离线 HTML Wiki
- Wiki 含搜索框 + 严重度/合规过滤器
- Wiki 含统计卡片（总数/高危/中危/低危）
- `--wiki-output <path>` 指定输出路径
- 支持 HTML + JSON 双格式输出
- 新增 8 测试全通过

---

## D30 · OAST 带外检测

**问题**：无回显漏洞（SSRF/XXE/SQL 盲注/RCE 盲注）缺乏确定性验证，传统特征匹配无法发现。

### 实施清单

| 序号 | 内容 | 文件 |
|------|------|------|
| 1 | CallbackStore 线程安全回调记录存储 | `lib/oast.py`（新增） |
| 2 | 交互 ID 生成 + payload URL/域名构建 | `lib/oast.py` |
| 3 | OASTServer 本地 HTTP 回调服务器（serve_forever） | `lib/oast.py` |
| 4 | OASTClient 客户端（生成 payload + 等待回调） | `lib/oast.py` |
| 5 | 6 种漏洞 payload 模板（SSRF/XXE/SQL盲注/RCE盲注/LDAP/命令注入） | `lib/oast.py` |
| 6 | CLI 参数 `--oast` / `--oast-server` / `--oast-host` / `--oast-port` | `main.py` |
| 7 | 测试：20 项存储/payload/客户端/服务器/HTTP 回调测试 | `tests/test_d30_d31_d32_d33.py` |

### 验收标准

- `--oast-server` 启动独立 OAST 回调服务器
- OASTClient.get_payload() 生成唯一回调 URL
- OASTClient.wait_callback() 等待回调确认漏洞
- HTTP 回调被正确记录到 CallbackStore
- 支持 6 种漏洞类型的 payload 模板
- 新增 20 测试全通过

---

## D31 · 业务逻辑漏洞检测

**问题**：传统扫描仅覆盖技术漏洞，无法发现 IDOR/越权/参数篡改/竞争条件等业务逻辑漏洞。

### 实施清单

| 序号 | 内容 | 文件 |
|------|------|------|
| 1 | LogicVuln / EndpointInfo 数据模型 | `lib/logic_scan.py`（新增） |
| 2 | IDORDetector 不安全直接对象引用检测（ID 参数识别 + 篡改测试） | `lib/logic_scan.py` |
| 3 | PrivilegeEscalationDetector 权限提升检测（管理端点发现 + 访问测试） | `lib/logic_scan.py` |
| 4 | ParameterTamperingDetector 参数篡改检测（价格/数量参数） | `lib/logic_scan.py` |
| 5 | RaceConditionDetector 竞争条件检测（并发请求） | `lib/logic_scan.py` |
| 6 | LogicScanner 聚合扫描器 + parse_endpoints_from_urls 端点解析 | `lib/logic_scan.py` |
| 7 | CLI 参数 `--logic-scan` / `--logic-endpoints` / `--logic-concurrency` | `main.py` |
| 8 | run_mode 集成业务逻辑扫描（主扫描后追加） | `main.py` |
| 9 | 测试：13 项 IDOR/越权/参数篡改/模型/端点解析测试 | `tests/test_d30_d31_d32_d33.py` |

### 检测类型

| 漏洞类型 | 检测策略 | 严重度 |
|----------|----------|--------|
| IDOR（越权访问） | 识别 ID 参数 → 篡改 ID → 对比响应 | high |
| 权限提升 | 发现管理端点 → 普通用户访问 | high |
| 参数篡改 | 识别价格/数量参数 → 篡改值 → 检查接受 | high |
| 竞争条件 | 并发 N 次请求 → 统计成功次数 | high |

### 验收标准

- `--logic-scan` 启用业务逻辑漏洞检测
- 支持 IDOR/越权/参数篡改/竞争条件 4 种检测
- 检测结果转换为 ScanResult 追加到主扫描结果
- 含修复详情 + 复现命令 + 合规映射
- 新增 13 测试全通过

---

## D32 · CVE/NVD 自动同步

**问题**：插件库的 CVE/CVSS/合规字段需手动维护，无法保持与 NVD 数据库同步。

### 实施清单

| 序号 | 内容 | 文件 |
|------|------|------|
| 1 | CVEInfo 数据模型（含 CWE → OWASP/等保 合规映射） | `lib/cve_sync.py`（新增） |
| 2 | NVD REST API 查询（query_nvd_api + parse_nvd_response） | `lib/cve_sync.py` |
| 3 | 本地缓存管理（24 小时 TTL，避免重复请求） | `lib/cve_sync.py` |
| 4 | lookup_cve / batch_lookup_cves 高层接口 | `lib/cve_sync.py` |
| 5 | extract_cve_ids_from_plugins 插件 CVE 提取 | `lib/cve_sync.py` |
| 6 | build_cve_update_report 同步报告生成 | `lib/cve_sync.py` |
| 7 | CLI 参数 `--cve-sync` / `--cve-id` / `--nvd-api-key` | `main.py` |
| 8 | 测试：13 项模型/解析/缓存/报告测试 | `tests/test_d30_d31_d32_d33.py` |

### CWE → 合规映射

| CWE | OWASP Top 10 | 等保 2.0 |
|-----|-------------|----------|
| CWE-89（SQL注入） | A03:2021 | 8.1.3 |
| CWE-79（XSS） | A03:2021 | 8.1.3 |
| CWE-22（路径遍历） | A01:2021 | 8.1.4 |
| CWE-502（反序列化） | A08:2021 | 8.1.3 |
| CWE-918（SSRF） | A10:2021 | 8.1.3 |
| CWE-862/863（授权缺失） | A01:2021 | 8.1.4 |

### 验收标准

- `--cve-sync` 扫描插件库 CVE 并同步 NVD 信息
- `--cve-id CVE-2024-XXXX` 查询单个 CVE
- 本地缓存 24 小时 TTL，避免重复请求
- 自动生成 CWE → OWASP/等保 合规标签
- 新增 13 测试全通过

---

## D33 · SIEM 集成（ECS/CEF 导出）

**问题**：扫描结果无法导出到 SIEM 系统（ELK/Splunk/QRadar），无法集成到安全运营中心。

### 实施清单

| 序号 | 内容 | 文件 |
|------|------|------|
| 1 | ECS (Elastic Common Schema) 格式渲染 | `lib/siem_export.py`（新增） |
| 2 | CEF (Common Event Format) 格式渲染 | `lib/siem_export.py` |
| 3 | LEEF (Log Event Extended Format) 格式渲染 | `lib/siem_export.py` |
| 4 | JSON Lines 通用格式渲染 | `lib/siem_export.py` |
| 5 | render_siem 统一导出接口 + parse_formats 格式解析 | `lib/siem_export.py` |
| 6 | export_to_files 多格式文件导出 | `lib/siem_export.py` |
| 7 | send_to_syslog / send_results_to_syslog Syslog 转发 | `lib/siem_export.py` |
| 8 | CLI 参数 `--siem-export` / `--siem-output` / `--siem-syslog` / `--siem-protocol` | `main.py` |
| 9 | run_mode 集成 SIEM 导出（报告生成后） | `main.py` |
| 10 | 测试：19 项格式/导出/Syslog/集成测试 | `tests/test_d30_d31_d32_d33.py` |

### 支持格式

| 格式 | 目标系统 | 特点 |
|------|----------|------|
| ECS | Elasticsearch/Kibana | Elastic Common Schema 标准 |
| CEF | ArcSight/Splunk | Common Event Format |
| LEEF | IBM QRadar | Log Event Extended Format |
| JSON | 通用 | JSON Lines，便于自定义集成 |

### 验收标准

- `--siem-export ecs,cef` 支持多格式同时导出
- `--siem-syslog 10.0.0.1:514` 发送到 Syslog 服务器
- 支持 UDP/TCP 协议
- 4 种格式（ECS/CEF/LEEF/JSON）全部覆盖
- 严重度/CVSS/CVE/合规字段完整导出
- 新增 19 测试全通过

---

## D34 · 异步扫描引擎

**问题**：单线程扫描无法充分利用多核 CPU，批量目标扫描耗时长；38 个同步插件无法直接转换为 async/await 形式。

### 实施清单

| 序号 | 内容 | 文件 |
|------|------|------|
| 1 | AsyncScanEngine 线程池引擎（start/stop/submit/map） | `lib/async_engine.py`（新增） |
| 2 | submit_async / map_async 异步方法（asyncio + run_in_executor） | `lib/async_engine.py` |
| 3 | scan_batch_targets 批量目标并发扫描（含进度回调） | `lib/async_engine.py` |
| 4 | scan_plugins_concurrent 插件并发执行（同一目标多插件） | `lib/async_engine.py` |
| 5 | async_http_get / async_http_post aiohttp 异步 HTTP（可选依赖） | `lib/async_engine.py` |
| 6 | benchmark_sync_vs_async 性能对比基准 | `lib/async_engine.py` |
| 7 | run_async_scan_mode 异步扫描模式入口 | `lib/async_engine.py` |
| 8 | CLI 参数 `--async` / `--async-workers` | `main.py` |
| 9 | 测试：13 项引擎/批量/并发/基准/集成测试 | `tests/test_d34_d35_d36_d37.py` |

### 技术方案

**线程池包装同步插件**：使用 `concurrent.futures.ThreadPoolExecutor` 包装 38 个同步插件，零侵入改造
- `submit(fn, *args)` 提交单个任务，返回 Future
- `map(fn, iterable)` 批量提交，按顺序返回结果
- `submit_async(fn, *args)` / `map_async(fn, iterable)` 结合 asyncio 事件循环
- 引擎统计：submitted/completed/failed/running

**aiohttp 可选依赖**：缺失时自动降级到同步 requests，不阻断主流程

### 验收标准

- `--async --async-workers 20` 启用 20 线程并发扫描
- 批量目标扫描性能显著优于串行
- 38 个同步插件无需任何修改即可并发执行
- 引擎正确统计任务状态
- 新增 13 测试全通过

---

## D35 · Web UI 控制台

**问题**：命令行交互门槛高，缺乏可视化扫描进度、漏洞列表、实时日志的统一界面。

### 实施清单

| 序号 | 内容 | 文件 |
|------|------|------|
| 1 | WEB_UI_TEMPLATE 单页 HTML 模板（内联 CSS+JS） | `lib/web_ui.py`（新增） |
| 2 | 扫描配置表单（URL/模式/模板/线程/认证） | `lib/web_ui.py` |
| 3 | 实时进度条 + 统计卡片（总数/高危/中危/低危） | `lib/web_ui.py` |
| 4 | 漏洞列表表格（严重度/CVE/URL/Payload） | `lib/web_ui.py` |
| 5 | 实时日志区域（WebSocket 推送） | `lib/web_ui.py` |
| 6 | 插件管理标签页（列表/启用/禁用） | `lib/web_ui.py` |
| 7 | 响应式设计（桌面/平板/移动端） | `lib/web_ui.py` |
| 8 | WebSocket 自动重连机制 | `lib/web_ui.py` |
| 9 | generate_web_ui / get_web_ui_info / run_web_ui_mode | `lib/web_ui.py` |
| 10 | CLI 参数 `--web-ui` / `--web-ui-output` / `--web-ui-api` | `main.py` |
| 11 | 测试：8 项生成/元素/标题/响应式/CSS/JS/信息测试 | `tests/test_d34_d35_d36_d37.py` |

### 技术方案

**单文件 HTML**：所有 CSS/JS 内联到单个 HTML 文件，零构建依赖，可离线打开
- 连接 D9/D11 已有的 FastAPI WebSocket 端点
- 前端原生 JS，无框架依赖（无 Vue/React）
- 深色主题 + 卡片式布局
- 移动端适配（媒体查询断点 768px）

### 验收标准

- `--web-ui` 生成单页 HTML 控制台
- 含扫描配置、进度、漏洞列表、日志、插件管理 5 个功能区域
- 响应式设计支持桌面/平板/移动端
- WebSocket 断线自动重连
- 新增 8 测试全通过

---

## D36 · 分布式任务队列

**问题**：单机扫描能力受限，无法横向扩展到多台 Worker 节点处理大规模目标列表。

### 实施清单

| 序号 | 内容 | 文件 |
|------|------|------|
| 1 | ScanTask 任务数据模型（to_dict/from_dict/to_json/from_json） | `lib/distributed.py`（新增） |
| 2 | TaskResult 结果数据模型 | `lib/distributed.py` |
| 3 | DistributedTaskQueue Redis 队列（LPUSH/BRPOP + 优先级） | `lib/distributed.py` |
| 4 | MasterNode 任务分发节点（distribute_tasks + collect_results + aggregate） | `lib/distributed.py` |
| 5 | WorkerNode 任务执行节点（run + heartbeat + stop） | `lib/distributed.py` |
| 6 | StandaloneDistributor 独立模式（无 Redis，本地多线程） | `lib/distributed.py` |
| 7 | CLI 参数 `--distributed` / `--redis-url` / `--worker-max-tasks` / `--distributed-timeout` | `main.py` |
| 8 | 测试：12 项任务模型/独立分发/Redis 模拟/聚合测试 | `tests/test_d34_d35_d36_d37.py` |

### 架构

| 角色 | 职责 | 依赖 |
|------|------|------|
| Master | 分发任务 + 收集结果 + 聚合统计 | Redis |
| Worker | 拉取任务 + 执行扫描 + 上报结果 + 心跳 | Redis |
| Standalone | 本地多线程分发（无需 Redis） | 无 |

### 技术方案

**Redis 队列**：使用 LPUSH 入队 + BRPOP 阻塞出队，支持优先级（high/normal/low 三队列）
- 任务状态：pending/running/completed/failed
- Worker 心跳：每 30 秒上报存活状态
- 无 Redis 时自动降级到 StandaloneDistributor（ThreadPoolExecutor 本地分发）

**无 Celery 依赖**：直接使用 redis-py 原生命令，降低部署复杂度

### 验收标准

- `--distributed standalone -f targets.txt` 本地多线程分布式扫描
- `--distributed master` 分发任务到 Redis 队列
- `--distributed worker` 拉取任务并执行扫描
- Master 节点聚合所有 Worker 结果
- 无 Redis 时 StandaloneDistributor 正常工作
- 新增 12 测试全通过

---

## D37 · 结果缓存

**问题**：重复扫描同一目标时重复执行全部插件，浪费时间和资源；缺乏缓存命中统计。

### 实施清单

| 序号 | 内容 | 文件 |
|------|------|------|
| 1 | generate_cache_key / generate_plugin_cache_key SHA256 键生成 | `lib/cache.py`（新增） |
| 2 | CacheStorage SQLite 持久层（get/set/delete/clear_all/clear_expired/get_stats） | `lib/cache.py` |
| 3 | ScanCache 高层接口（扫描级 + 插件级缓存） | `lib/cache.py` |
| 4 | cached_scan 透明缓存装饰器 | `lib/cache.py` |
| 5 | TTL 过期机制（自动清理过期记录） | `lib/cache.py` |
| 6 | 命中率统计（hit_count/miss_count/hit_rate） | `lib/cache.py` |
| 7 | get_by_target 按目标查询缓存历史 | `lib/cache.py` |
| 8 | CLI 参数 `--cache` / `--cache-ttl` / `--cache-db` / `--cache-stats` / `--cache-clear` / `--cache-clear-all` | `main.py` |
| 9 | 测试：28 项键生成/存储/扫描/装饰器/集成测试 | `tests/test_d34_d35_d36_d37.py` |

### 技术方案

**缓存键**：`SHA256(target + plugin_config + scan_mode)[:16]`，确保同目标不同配置不冲突
- 键归一化：URL 末尾斜杠统一、域名大小写统一
- TTL 默认 1 小时，可通过 `--cache-ttl` 调整

**SQLite 存储**：标准库 `sqlite3`，零外部依赖
- 表 `cache_entries`：cache_key / target / result(JSON) / created_at / expires_at / plugin_name / hit_count
- WAL 模式，支持并发读
- `get()` 时自动检查过期并删除

**透明缓存装饰器**：`@cached_scan(cache, target_arg='target')` 让插件自动缓存，无需修改插件逻辑

### 验收标准

- `--cache --cache-ttl 7200` 启用 2 小时 TTL 缓存
- 重复扫描同目标命中缓存，跳过插件执行
- `--cache-stats` 显示命中率/总数/过期数
- `--cache-clear <target>` 清除指定目标缓存
- `--cache-clear-all` 清除全部缓存
- 缓存键归一化（斜杠/大小写）
- 新增 28 测试全通过

---

## D10+ 路线图完成总结

**全方向落地状态**：D10-D16（第一批 7 方向）+ D18/D19/D24/D27（第二批 4 方向）+ D20/D21/D22/D26（第三批 4 方向）+ D23/D25/D28/D29（第四批 4 方向）+ D30/D31/D32/D33（第五批 4 方向）+ D34/D35/D36/D37（第六批 4 方向）全部 27 个方向已完成，所有方向测试零回归。

### 关键交付物

| 方向 | 核心交付 | 新增文件 | 修改文件 | 新增测试 |
|------|----------|----------|----------|----------|
| D10 | 文档与实现对齐 | 0 | 3 | 0 |
| D11 | API Key 鉴权 + SQLite 持久化 | 2 | 4 | 17 |
| D12 | CVE/CVSS/合规映射（38 POC 全覆盖） | 1 | 41 | 28 |
| D13 | 代理池 + 三种轮换策略 + 自动剔除 | 2 | 2 | 17 |
| D14 | BFS 爬虫 + 子域名枚举 + JS 端点提取 | 4 | 2 | 77 |
| D15 | 4 CMS + 4 WAF 扩充 | 0 | 3 | 6 |
| D16 | Prometheus 指标 + Docker Compose 监控栈 | 4 | 3 | 22 |
| D18 | 修复详情增强（38 POC fix_detail） | 1 | 42 | 44 |
| D24 | 漏洞复现命令（38 POC reproduce） | 0 | 0 | 0 |
| D19 | 扫描模板（quick/deep/compliance/dengbao） | 2 | 1 | 44 |
| D27 | YAML 配置文件加载 | 1 | 1 | 0 |
| D20 | 增量扫描与差异对比 | 2 (lib/diff_scan.py, tests/test_d20_d21_d22_d26.py) | 1 (main.py) | 14 |
| D22 | SARIF 2.1.0 报告格式 | 1 (core/report_sarif.py) | 2 (core/report.py, main.py, tests/test_d8_cli.py) | 13 |
| D21 | 告警通知（Webhook/邮件/钉钉/企微/飞书） | 1 (lib/notifier.py) | 1 (main.py) | 15 |
| D26 | 认证扫描增强（Cookie/Token/Bearer/自动登录） | 1 (lib/auth_scan.py) | 1 (main.py) | 13 |
| D23 | 国际化（i18n 中英文报告） | 1 (lib/i18n.py) | 1 (main.py) | 14 |
| D25 | 插件 SDK（模板生成+验证+枚举+文档） | 1 (lib/plugin_sdk.py) | 1 (main.py) | 13 |
| D28 | CI/CD 集成（GitHub/GitLab/Jenkins 模板+退出码） | 1 (lib/ci_runner.py) | 1 (main.py) | 18 |
| D29 | 漏洞知识库（离线 HTML Wiki + JSON API） | 1 (lib/vuln_wiki.py) | 1 (main.py) | 8 |
| D30 | OAST 带外检测（回调服务器+6 种 payload 模板） | 1 (lib/oast.py) | 1 (main.py) | 20 |
| D31 | 业务逻辑漏洞检测（IDOR/越权/参数篡改/竞争条件） | 1 (lib/logic_scan.py) | 1 (main.py) | 13 |
| D32 | CVE/NVD 自动同步（API 查询+缓存+合规映射） | 1 (lib/cve_sync.py) | 1 (main.py) | 13 |
| D33 | SIEM 集成（ECS/CEF/LEEF/JSON 四格式+Syslog） | 1 (lib/siem_export.py) | 1 (main.py) | 19 |
| D34 | 异步扫描引擎（ThreadPoolExecutor+aiohttp） | 1 (lib/async_engine.py) | 1 (main.py) | 13 |
| D35 | Web UI 控制台（单页 HTML+WebSocket） | 1 (lib/web_ui.py) | 1 (main.py) | 8 |
| D36 | 分布式任务队列（Redis Master-Worker+独立模式） | 1 (lib/distributed.py) | 1 (main.py) | 12 |
| D37 | 结果缓存（SQLite+TTL+命中率统计） | 1 (lib/cache.py) | 1 (main.py) | 28 |

### 新增能力概览

- **D11 生产化**：`--api-key` 鉴权 + `--cors-origins` 收紧 + `--db-path` SQLite 持久化
- **D12 合规化**：38 个 POC 全部补全 CVE/CVSS v3.1/合规映射（等保 2.0 + OWASP Top 10 2021）
- **D13 反封禁**：`--proxy-file` 代理池，round-robin/random/least-fail 三种轮换策略
- **D14 资产发现**：`--crawl` BFS 爬虫 + `--subdomain` 子域名枚举 + `--js-extract` JS 端点提取
- **D15 识别精度**：新增 ruoyi-cloud + jeecgboot CMS，新增 AWS WAF / F5 ASM / Akamai / Imperva WAF
- **D16 运维部署**：`/api/system/metrics` Prometheus 端点 + docker-compose 监控栈
- **D18 修复详情**：38 个 POC 全部补全具体修复代码（升级方案+代码 diff+配置加固+WAF 规则+合规映射）
- **D24 复现命令**：38 个 POC 全部补全复现命令（curl 探针+利用 PoC+预期响应）
- **D19 扫描模板**：`--template quick/deep/compliance/dengbao` 一键选择预设策略
- **D27 YAML 配置**：`--config scan.yml` 加载 YAML 配置文件，CLI 参数优先
- **D20 增量扫描**：`--save-baseline` 保存基线，`--diff <old.json>` 对比差异（新增/修复/未变/变化）
- **D22 SARIF 报告**：`--report-format sarif` 生成 SARIF 2.1.0 格式，支持 GitHub Code Scanning
- **D21 告警通知**：`--notify webhook/dingtalk/wechat/feishu/email` 扫描完成自动推送结果
- **D26 认证扫描**：`--auth cookie/bearer/header/basic` + `--auth-file` + `--auth-login` 认证信息注入
- **D23 国际化**：`--lang zh|en` 中英文报告切换，支持标题/状态/严重度/CSV 表头翻译
- **D25 插件 SDK**：`--plugin-init` 生成插件模板 + `--plugin-check` 验证 + `--plugin-list` 枚举
- **D28 CI/CD 集成**：`--ci` 模式按严重度阈值退出 + `--ci-init github/gitlab/jenkins` 生成流水线配置
- **D29 漏洞知识库**：`--wiki` 生成离线 HTML Wiki（含搜索/过滤/统计卡片）+ JSON API
- **D30 OAST 带外检测**：`--oast-server` 启动回调服务器 + `--oast` 启用带外检测（SSRF/XXE/SQL盲注/RCE盲注）
- **D31 业务逻辑检测**：`--logic-scan` 检测 IDOR/越权/参数篡改/竞争条件（依赖 D26 认证扫描）
- **D32 CVE 同步**：`--cve-sync` 从 NVD 同步 CVE 信息到插件库 + `--cve-id` 查询单个 CVE
- **D33 SIEM 集成**：`--siem-export ecs/cef/leef/json` 多格式导出 + `--siem-syslog` Syslog 转发
- **D34 异步扫描引擎**：`--async` 启用 ThreadPoolExecutor 并发扫描 + `--async-workers N` 控制并发数
- **D35 Web UI 控制台**：`--web-ui` 生成单页 HTML 控制台（WebSocket 实时进度+漏洞列表+日志）
- **D36 分布式任务队列**：`--distributed master/worker/standalone` + `--redis-url` Redis 队列
- **D37 结果缓存**：`--cache` 启用 SQLite 缓存 + `--cache-stats` 统计 + `--cache-clear` 清除

### 使用示例

```bash
# D19：使用扫描模板
python main.py --template deep -u http://target/ --report reports/
python main.py --template-list  # 列出所有可用模板

# D27：使用 YAML 配置文件
python main.py --config scan.yml

# D20：增量扫描与差异对比
python main.py -u http://target/ --report reports/ --save-baseline  # 保存基线
python main.py -u http://target/ --report reports/ --diff reports/baseline.json  # 对比基线
python main.py --diff-only reports/old.json reports/new.json  # 仅对比不扫描

# D22：SARIF 报告（GitHub Code Scanning）
python main.py -u http://target/ --report reports/ --report-format sarif
# 上传到 GitHub：gh code-scanning upload --sarif reports/report.sarif

# D21：告警通知
python main.py -u http://target/ --notify webhook=https://hooks.example.com/scan
python main.py -u http://target/ --notify dingtalk=https://oapi.dingtalk.com/robot/send?access_token=xxx
python main.py -u http://target/ --notify email=security@example.com

# D26：认证扫描
python main.py -u http://target/ --auth cookie="JSESSIONID=abc123; token=xyz"
python main.py -u http://target/ --auth bearer=eyJhbGciOiJIUzI1NiJ9
python main.py -u http://target/ --auth-file cookies.txt
python main.py -u http://target/ --auth-login admin:password123

# D23：国际化（中英文报告）
python main.py -u http://target/ --lang en --report reports/  # 英文报告
python main.py -u http://target/ --lang zh --report reports/  # 中文报告（默认）

# D25：插件 SDK（自定义插件开发）
python main.py --plugin-init my_rce_vuln --category ruoyi     # 生成插件模板
python main.py --plugin-check plugins/ruoyi/my_rce_vuln.py    # 验证插件
python main.py --plugin-list                                   # 列出所有插件

# D28：CI/CD 集成
python main.py -u http://target/ --ci --severity-threshold high  # CI 模式扫描
python main.py --ci-init github                                  # 生成 GitHub Actions 配置
python main.py --ci-init gitlab                                  # 生成 GitLab CI 配置
python main.py --ci-init jenkins                                 # 生成 Jenkinsfile

# D29：漏洞知识库
python main.py --wiki                                            # 生成 HTML Wiki
python main.py --wiki --wiki-output wiki/vuln_wiki.html          # 指定输出路径

# D30：OAST 带外检测
python main.py --oast-server                                     # 启动 OAST 回调服务器
python main.py -u http://target/ --oast --oast-port 5555         # 启用 OAST 带外检测

# D31：业务逻辑漏洞检测（需认证）
python main.py -u http://target/ --logic-scan --auth-login admin:password
python main.py -u http://target/ --logic-scan --logic-endpoints endpoints.txt

# D32：CVE/NVD 自动同步
python main.py --cve-sync                                        # 同步所有插件 CVE
python main.py --cve-id CVE-2024-1234                            # 查询单个 CVE
python main.py --cve-sync --nvd-api-key YOUR_API_KEY             # 使用 API Key 提升速率

# D33：SIEM 集成
python main.py -u http://target/ --siem-export ecs,cef --siem-output reports/siem/
python main.py -u http://target/ --siem-export cef --siem-syslog 10.0.0.1:514
python main.py -u http://target/ --siem-export ecs --siem-syslog siem.local:514 --siem-protocol tcp

# D34：异步扫描引擎
python main.py -u http://target/ --async --async-workers 20          # 异步并发扫描
python main.py -f targets.txt --async --async-workers 50             # 批量目标异步扫描

# D35：Web UI 控制台
python main.py --web-ui                                              # 生成控制台到默认路径
python main.py --web-ui --web-ui-output console/index.html           # 指定输出路径
python main.py --web-ui --web-ui-api http://localhost:8000           # 指定 API 地址

# D36：分布式任务队列
python main.py --distributed standalone -f targets.txt               # 本地多线程分布式
python main.py --distributed master -f targets.txt --redis-url redis://10.0.0.1:6379
python main.py --distributed worker --redis-url redis://10.0.0.1:6379 --worker-max-tasks 100

# D37：结果缓存
python main.py -u http://target/ --cache --cache-ttl 7200            # 启用缓存（2 小时 TTL）
python main.py --cache-stats                                          # 查看缓存统计
python main.py --cache-clear http://target/                           # 清除指定目标缓存
python main.py --cache-clear-all                                      # 清除全部缓存

# 组合使用（配置文件 + 模板 + 认证 + OAST + 业务逻辑 + SIEM + 异步 + 缓存 + CI 模式）
python main.py --config scan.yml --template deep --auth-file cookies.txt \
  --oast --logic-scan --siem-export ecs,cef --siem-output reports/siem/ \
  --async --async-workers 20 --cache --cache-ttl 7200 \
  --ci --severity-threshold high --lang en --report reports/
```

### 累计测试统计

- D1-D9：380 测试
- D10-D16 新增：167 测试
- D18+D24 新增：44 测试
- D19+D27 新增：44 测试
- D20+D21+D22+D26 新增：55 测试
- D23+D25+D28+D29 新增：55 测试
- D30+D31+D32+D33 新增：65 测试
- D34+D35+D36+D37 新增：61 测试
- **总计：871 测试全部通过，零回归**

