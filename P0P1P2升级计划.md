# Ruoyi-Scan 做深若依升级计划

> **2026-07-18 重大调整**：原"P0→P1→P2 多 CMS 扩充计划"已废弃。
> 项目转向**专注若依做深**路线，thinkphp/weaver/shiro/struts2/nuclei 已抽离至 `../cms-scan-extras/`。
> 本文件记录新的做深计划，替代旧版 P0P1P2 计划。

---

## 📋 当前基线（2026-07-18 抽离后）

| 项 | 值 |
|----|----|
| 保留 CMS | `ruoyi`（16 POC）+ `spring`（14 POC）+ `common`（通用漏洞包） |
| 已抽离 CMS | thinkphp / weaver / shiro / struts2 / nuclei → `../cms-scan-extras/` |
| 引擎/路由/指纹 | 插件化分层完好，`Router.mapping` 仅 ruoyi/spring |
| 已修 bug | 限速锁并发退化（`_rate_limit` 锁外 sleep）、`--cms` choices 收窄 |
| 待修硬伤 | 签名 marker 循环验证（nacos_unauth / file_read_path） |

---

## 🎯 做深若依：核心目标

**判定标准**：每个若依 POC 在真实 RuoYi 靶场上能说出命中率，CONFIRMED 结果**敢直接写报告**。

### D1 · 登录链打通（预计 2-3 天，高优先级）

**问题**：`plugins/ruoyi/file_read_time.py` 用硬编码 `settings.JOB_JSESSIONID`，真实若依需先登录拿会话。

**方案**：
- 新增 `core/auth_chain.py`：若依登录链编排器
  - RuoYi v4（Session）：POST `/login` → 提取 JSESSIONID → 后续请求带 Cookie
  - RuoYi v5（JWT）：POST `/login` → 提取 token → 后续请求加 `Authorization: Bearer <token>`
- 修改 `plugins/ruoyi/file_read_time.py` / `job_rce.py` / `thymeleaf_ssti.py` 等需鉴权 POC：先走登录链拿会话
- `config/settings.py` 新增 `RuoYiAuth` 配置（默认口令 admin/admin123、可选自定义）

**验收**：在 `lab/real-ruoyi`（开启鉴权）上跑 `file_read_time` / `job_rce`，CONFIRMED。

### D2 · 多版本 POC 适配（预计 3-4 天，高优先级）

**问题**：若依 4.2 / 4.4 / 4.6 / 4.7 / v5 各版本漏洞点与接口路径有差异，单 POC 打天下必漏报。

**方案**：
- `lib/ruoyi_versions.py`：版本指纹库（响应特征 → 版本范围）
  - `/system/info` 返回的 RuoYi 版本字符串
  - `/actuator/info` 或静态资源路径中的版本 hash
  - 默认首页 footer 的版本号
- 每个 POC 标注 `affected_versions`（如 `>=4.2,<4.6`），指纹识别版本后只跑对应 POC
- 重点覆盖的差异点：
  - `/system/role/list` 的 `params[dataScope]` 注入在 4.6+ 已修
  - `/monitor/job/edit` 白名单在 4.7+ 收紧
  - `/common/upload` 扩展名校验在 4.6+ 加强
  - v5 用 JWT 鉴权，接口路径前缀 `/prod-api/`

**验收**：在 4.2 / 4.7.8 / v5 三个版本上分别扫描，POC 命中范围与 `affected_versions` 一致。

### D3 · 验证码处理（预计 2 天，中优先级）

**问题**：`default_password.py` 遇验证码直接 `UNKNOWN`，真实若依几乎都开验证码。

**方案**：
- 检测 `/captcha/image` 是否存在 + 响应是否为图片
- 分支处理：
  - 无验证码：走原逻辑
  - 验证码可绕过（旧版 RuoYi 4.2- 验证码不校验）：直接爆破
  - 验证码必校验：接 OCR（标准库做不了，**允许破"零依赖"引入 ddddocr 或类似轻量库**）
- `config/settings.py` 新增 `captcha_mode`：`auto` / `ocr` / `skip`

**验收**：在 `lab/real-ruoyi` 开启验证码模式下，`default_password` 能 CONFIRMED。

### D4 · real-ruoyi 自动化验收（预计 1-2 天，高优先级）

**问题**：`lab/real-ruoyi` 是真 Java 应用，目前 CI 设为手动触发（重负载）。签名 marker 循环验证无真实保证。

**方案**：
- 改造 `lab/real-ruoyi` 启动脚本：CI 中用 Docker Compose 一键起 MySQL + RuoYi
- `scripts/run_e2e.py` 新增 `--real-ruoyi` 模式：自动等 Java 进程就绪、自动跑扫描、断言 CONFIRMED 数
- 删除 `plugins/ruoyi/nacos_unauth.py` / `file_read_path.py` 的签名 marker 判定，改为真实响应特征判定
  - `nacos_unauth`：判定 `/nacos/v1/auth/users` 返回 JSON 含 `username` / `password` 字段
  - `file_read_path`：判定响应含 `root:x:0:` 或 `daemon:x:1:` 等真实文件特征
- 同步修改 `lab/server.py`：去掉 `NACOS_UNAUTH_MARKER` / `FILE_READ_PATH_MARKER` 魔法常量，返回真实漏洞响应

**验收**：CI 自动跑 real-ruoyi，所有若依 POC 在真 Java 应用上 CONFIRMED，README 写明各 POC 真实命中数。

### D5 · 误报率实测（预计 1 天，高优先级）

**问题**：当前无假阳率数据，"敢写报告"无硬指标。

**方案**：
- 准备 5-10 个非若依 Java 站点：Spring 纯净站、JeecgBoot、JFinal、纯静态站
- 跑 `python main.py -p <target>` 各一次
- 统计：假阳数 / 总扫描数 = 假阳率
- 假阳率 > 5% 的 POC 必须修判定逻辑

**验收**：README 写明假阳率实测数据（如"10 个非若依站，0 假阳"）。

---

## 🔧 Spring 协同做中（预计 2 天，中优先级）

Spring 与若依生态强相关，保持当前 14 POC，不做大扩充，只补真实验证：

- `lab/real-spring` 已存在（Flask 模拟真实响应），CI 已自动跑
- 补充：在真实 Spring Boot 2.x / 3.x 应用上验证（可用 vulhub 镜像）
- 不新增 POC，专注降低现有 POC 假阴/假阳

---

## 🚫 已废弃计划（原 P0P1P2，保留历史参考）

以下阶段已随 2026-07-18 抽离废弃，不再执行：

| 原阶段 | 状态 | 去向 |
|--------|------|------|
| P0-1 限速修复 | ✅ 已完成（2026-07-18 抽离时同步修复锁外 sleep） | `core/engine.py` |
| P0-2 实时输出 | ✅ 已完成（早期版本） | `main.py` |
| P0-3 `--timeout` | ✅ 已完成（早期版本） | `main.py` |
| P0-4 `--cms` | ✅ 已完成，choices 收窄为 ruoyi/spring | `main.py` |
| P1-A 通用漏洞包 | ✅ 已完成 | `plugins/common/` |
| P1-B 口令字典分级 | ⏸ 暂缓（做深阶段可重启） | — |
| P1-C WAF 探测 | ⏸ 暂缓 | — |
| P1-D Shiro | ❌ 废弃，迁至 cms-scan-extras | `../cms-scan-extras/plugins/shiro/` |
| P1-E Struts2 | ❌ 废弃，迁至 cms-scan-extras | `../cms-scan-extras/plugins/struts2/` |
| P1-F 4 CMS 扩充 | ❌ 废弃（thinkphp/weaver 部分） | `../cms-scan-extras/` |
| P2-A 端口扫描 | ✅ 已完成（`--portscan` / `--ports`，调用 `core/portscan.py`） | `core/portscan.py`、`main.py` |
| P2-B 被动扫描 | ✅ 已完成（`--passive` / `--passive-host` / `--passive-port`，调用 `core/proxy_server.py`） | `core/proxy_server.py`、`main.py` |
| P2-C Nuclei 兼容 | ❌ 废弃，迁至 cms-scan-extras | `../cms-scan-extras/core/nuclei_adapter.py` |

---

## 🔒 架构红线（做深阶段不得违反）

1. **零改引擎/路由主体**：新增 POC 仅加插件文件，`core/engine.py` / `core/router.py` 不改主逻辑
2. **判定三态**：CONFIRMED / SAFE / UNKNOWN，网络异常绝不判 SAFE
3. **真实响应优先**：新 POC 判定基于真实漏洞响应特征，不依赖签名 marker
4. **版本适配**：每个 POC 标注 `affected_versions`，指纹识别版本后定向跑
5. **验证闭环**：每个 POC 在 `lab/real-ruoyi` 上 CONFIRMED + 在非若依站上零假阳
6. **中文注释 + ANSI 配色 + 89 字符分隔线**：延续 AGENTS.md 规范

---

## 📊 做深前后对比目标

| 指标 | 当前（2026-07-18） | 做深目标 |
|------|---------------------|----------|
| 若依 POC 真实验收率 | ~30%（仅 5/16 在 real-ruoyi 验过） | 100% |
| 若依 POC 假阳率 | 未知 | < 5%（10 个非若依站实测） |
| 版本覆盖 | 单 payload 打天下 | 4.2 / 4.7 / v5 三版本适配 |
| 登录链 | 硬编码 JSESSIONID | v4 Session + v5 JWT 双链路 |
| 验证码处理 | 直接 UNKNOWN | OCR / 绕过 / 跳过 三模式 |
| real-ruoyi CI | 手动触发 | 自动触发 + CONFIRMED 数断言 |

---

## 🚀 执行顺序建议

1. **D4 real-ruoyi 自动化验收**（先建真实验收基线，后续改动才有底气）
2. **D1 登录链打通**（解决最大漏报源）
3. **D2 多版本适配**（解决版本差异漏报）
4. **D5 误报率实测**（拿到"敢写报告"硬指标）
5. **D3 验证码处理**（解决 default_password UNKNOWN）
6. Spring 协同做中（穿插进行）

每个 D 阶段完成后：`pytest -q` 全绿 + `regression_ruoyi.py` 全绿 + real-ruoyi 扫描 CONFIRMED 数不回退。

---

## D8 · 报告增强（PDF/Word/Excel + 结果去重聚合）— ✅ 已完成

> **阶段定位**：补齐 PDF/Word/Excel 报告输出能力，解决同目标内多插件命中同一漏洞的重复问题。
> **约束**：零系统依赖（reportlab/python-docx/openpyxl 均为纯 manylinux wheel），不破坏现有 HTML/JSON/CSV 输出。

### 库选型

| 报告格式 | 选型 | 依赖包 | 主要理由 |
|---|---|---|---|
| PDF | reportlab | `reportlab>=4.0` | 零系统依赖 + 内置 STSong-Light CJK 字体 + Platypus 分节 |
| Word | python-docx | `python-docx>=1.1` | 数据驱动 + 无需模板文件 + 样式代码可控 |
| Excel | openpyxl | `openpyxl>=3.1` | 可回读 + 原生图表 + 条件格式 |

### 结果去重聚合机制

**指纹构成**：`fp = sha1(normalized_endpoint | vuln_type | payload_class)[:16]`

- `vuln_type` / `payload_class` 是新约定的 `ScanResult.extra` 字段（不破坏 dataclass）
- 聚合层位于 `ReportBuilder` 渲染前（不破坏 `ScanEngine` 契约，可 `--no-dedup` 绕过）
- `AggregatedVuln` 鸭子类型兼容 `ScanResult`，现有渲染方法零改动

**聚合规则**：severity 取最高、status 取最差、name 取 vuln_type 中文名、evidence 合并去重、fix 取最长

### 实施清单

| 阶段 | 内容 | 状态 |
|------|------|------|
| D8.1 | `core/dedup.py` + `ReportBuilder` 集成 + 插件 extra 字段（15 测试） | ✅ |
| D8.2 | `core/report_pdf.py`（reportlab + STSong-Light CJK，8 测试） | ✅ |
| D8.3 | `core/report_docx.py`（python-docx，10 测试） | ✅ |
| D8.4 | `core/report_xlsx.py`（openpyxl 三 Sheet + 饼图，12 测试） | ✅ |
| D8.5 | CLI `--report-format` / `--no-dedup` + 依赖管理 + 降级策略（20 测试） | ✅ |
| D8.6 | CI `report-format` job + 文档收尾 | ✅ |

### 新增文件（7 个）

| 文件 | 说明 |
|------|------|
| `core/dedup.py` | 去重聚合核心（指纹 + 聚合 + DedupReport） |
| `core/report_pdf.py` | PDF 报告（reportlab Platypus） |
| `core/report_docx.py` | Word 报告（python-docx） |
| `core/report_xlsx.py` | Excel 报告（openpyxl 三 Sheet + 饼图） |
| `tests/test_dedup.py` | 去重聚合 15 用例 |
| `tests/test_report_pdf.py` | PDF 生成 8 用例 |
| `tests/test_report_docx.py` | Word 生成 10 用例 |
| `tests/test_report_xlsx.py` | Excel 生成 12 用例 |
| `tests/test_d8_cli.py` | CLI 集成 + 降级 20 用例 |

### 修改文件

| 文件 | 改动点 |
|------|--------|
| `core/report.py` | `dedup` 参数 + `_effective_results()` + `render_all(formats=)` |
| `plugins/base.py` | `_build_result()` 辅助方法 |
| `plugins/ruoyi/file_read.py` | `extra={'vuln_type':'arbitrary_file_read'}` |
| `plugins/ruoyi/file_read_path.py` | 同上（与 file_read 一致才合并） |
| `main.py` | `--report-format` / `--no-dedup` + `_parse_report_formats()` |
| `requirements.txt` | 拆分主依赖 + 可选依赖区 |
| `.github/workflows/ci.yml` | 新增 `report-format` job |
| `README.md` / `agents.md` | 文档更新 |

### 降级策略

`render_all` 渲染 PDF/Word/Excel 时 try-import，失败则跳过该格式并打印警告（不崩溃）：
- 全依赖安装：生成 6 文件（HTML/JSON/CSV/PDF/Word/Excel）
- 仅主依赖：生成 3 文件（HTML/JSON/CSV）+ 3 条降级警告

### 测试总量

D8 新增 65 个测试（15 dedup + 8 PDF + 10 Word + 12 Excel + 20 CLI），全量 149 测试通过，零回归。

---

## D6 · 漏洞利用链编排（DAG 拓扑 + 条件分支 + 失败策略）— ✅ 已完成

> **阶段定位**：将独立执行的原子 POC 组合为端到端攻击链，支持 DAG 拓扑排序、条件分支、失败策略（abort/continue/fallback）、上下文共享（facts/secrets）。
> **约束**：零改 core/engine.py / core/router.py / core/models.py / plugins/base.py 主逻辑；链引擎独立模块，通过 PluginBase.verify(target, session) 接口调用现有插件。

### 核心数据结构

| 类 | 职责 |
|---|---|
| `ChainStep` | 链节点（plugin_cls + condition + on_fail + depends_on + inputs/outputs） |
| `ChainEdge` | 依赖边（显式声明，用于 DAG 拓扑排序） |
| `ChainDef` | 链定义（steps + edges + meta），含 `validate()` 环检测 |
| `ChainContext` | 执行上下文（results/facts/secrets，线程安全，secrets 脱敏） |
| `ChainEngine` | 执行引擎（Kahn 拓扑排序 + 节点执行 + abort 传播 + 状态聚合） |
| `ChainResult` | 链执行结果（状态 + 节点结果 + 证据，可转 `ScanResult(kind='chain')`） |

### 失败策略

| 策略 | 行为 |
|------|------|
| `abort` | 关键节点失败则整链中断，下游全 skipped |
| `continue` | 忽略失败，继续执行下游 |
| `fallback` | 失败时执行 fallback_steps |

### 链整体状态（四级，部分成功不升级为 CONFIRMED）

| 状态 | 判定条件 |
|------|----------|
| `CONFIRMED` | 所有关键节点 success |
| `PARTIAL` | 部分关键节点 success，部分 failed/skipped |
| `BLOCKED` | 关键节点 abort 导致链中断 |
| `UNKNOWN` | 存在 UNKNOWN 且无 failed（无法判定） |

### 三条经典链

| 链名 | 攻击路径 | 步骤数 |
|------|----------|--------|
| `ruoyi_sql_to_rce` | SQL注入 → 文件读取配置 → 定时任务 RCE | 3 |
| `ruoyi_defaultpw_to_webshell` | 默认口令 → 登录链 → 任意文件上传 → webshell | 2 |
| `ruoyi_nacos_to_dbcreds` | Nacos 未授权 → 配置泄露 → 数据库凭证 | 2 |

### 实施清单

| 阶段 | 内容 | 状态 |
|------|------|------|
| D6.1 | `core/chain.py` 编排器核心 + 21 单测 | ✅ |
| D6.2 | `main.py --chain` + `chains/registry.py` + 9 CLI 测试 | ✅ |
| D6.3 | 3 链定义 + 3 链专用插件 + 9 场景 mock 测试 | ✅ |
| D6.4 | 并行分支（已通过 DAG 多依赖实现基本并行） | ✅ |

### 新增文件（11 个）

| 文件 | 说明 |
|------|------|
| `core/chain.py` | 链编排器核心（ChainStep/ChainDef/ChainContext/ChainEngine/ChainResult） |
| `chains/__init__.py` | 链包初始化 |
| `chains/registry.py` | 链注册表（list_chains/get_chain/register_chain） |
| `chains/ruoyi_sql_to_rce.py` | 链 1 定义 |
| `chains/ruoyi_defaultpw_to_webshell.py` | 链 2 定义 |
| `chains/ruoyi_nacos_to_dbcreds.py` | 链 3 定义 |
| `plugins/chain/__init__.py` | 链专用插件包 |
| `plugins/chain/sql_to_rce_steps.py` | 链 1 三个原子步骤插件 |
| `plugins/chain/defaultpw_to_webshell_steps.py` | 链 2 两个原子步骤插件 |
| `plugins/chain/nacos_to_dbcreds_steps.py` | 链 3 两个原子步骤插件 |
| `tests/test_chain.py` | 编排器 21 单测 |
| `tests/test_d6_cli.py` | CLI 集成 9 测试 |
| `tests/test_d6_chains.py` | 三链场景 9 测试 |

### 修改文件

| 文件 | 改动点 |
|------|--------|
| `main.py` | `--chain` / `--chain-list` 参数 + `run_chain_mode()` + 模式分发 |
| `README.md` / `agents.md` | 文档更新 |

### 测试总量

D6 新增 39 个测试（21 编排器 + 9 CLI + 9 场景），全量 188 测试通过，零回归。

---

## D7 · WAF 自动绕过执行（四层策略 + 三态判定保护矩阵）— ✅ 已完成

> **阶段定位**：将 `detect_waf()` 的"检测+文字提示"升级为"检测+策略执行+结果标记"，WAF 命中后自动尝试 11 种绕过策略，恢复被 WAF 拦截的漏报。
> **约束**：零改 `core/session.py`（组合模式包装）、零改 `core/models.py`（复用 `extra` 字段）、`waf_bypass_coordinator=None` 时引擎行为与现状逐字一致。

### 四层绕过技术体系

| 层次 | 技术 | 适用 WAF |
|------|------|---------|
| **L1 payload 变形** | 内联注释 `/**/`、MySQL 版本注释 `/*!50000*/`、大小写混淆、BETWEEN 替换 | safedog/modsecurity/aliyun_waf |
| **L2 编码绕过** | URL 编码、双重 URL 编码 | modsecurity/chaitin |
| **L3 协议层** | 分块传输(Chunked)、HTTP/1.0 降级、HPP 参数污染、Googlebot 伪装 | cloudflare/aliyun_waf/tencent_waf |
| **L4 源站直连** | SSL 证书 SAN(crt.sh)、DNS 历史记录、响应头泄漏、子域名枚举 | cloudflare/baidu_waf(所有 CDN 类) |

### 三态判定保护矩阵

| 原 verify 结果 | WAF 拦截? | 绕过结果 | 最终状态 | 合规性 |
|---------------|----------|---------|---------|--------|
| CONFIRMED | — | 不触发 | CONFIRMED | 铁律：已确认不绕过 |
| SAFE | 否(真 SAFE) | 不触发 | SAFE | 不误绕 |
| SAFE | 是(被拦假 SAFE) | 绕过成功 | CONFIRMED | 恢复漏报（核心价值） |
| SAFE | 是 | 绕过失败 | SAFE + extra 标记 | 可审计 |
| UNKNOWN | 是 | 绕过成功 | CONFIRMED | 恢复漏报 |
| UNKNOWN | 是 | 绕过失败 | UNKNOWN + extra 标记 | 不降级 |
| 任意 | — | 绕过异常 | 原状态 + UNKNOWN 兜底 | 绝不判 SAFE |

### 11 种绕过策略

| 策略 ID | 策略名 | 技术层 | 适用 WAF | 适用漏洞类型 |
|---------|--------|--------|---------|-------------|
| BP-GEN-1 | 大小写混淆 | L1 | 通用 | sqli/xss/rce |
| BP-GEN-2 | URL 编码 | L2 | 通用 | sqli/xss/rce/file_read |
| BP-GEN-3 | 分块传输 | L3 | 通用 | sqli/rce/file_read |
| BP-SD-1 | 内联注释变形 | L1 | safedog/aliyun_waf/modsecurity | sqli |
| BP-SD-1b | MySQL 版本注释 | L1 | safedog/aliyun_waf | sqli |
| BP-SD-3 | BETWEEN 替换 | L1 | safedog/modsecurity | sqli |
| BP-CP-1 | 双重 URL 编码 | L2 | chaitin/modsecurity | sqli/rce |
| BP-ALI-3 | HPP 参数污染 | L3 | aliyun_waf/tencent_waf | sqli |
| BP-CP-2b | HTTP/1.0 降级 | L3 | chaitin/knownsec | 全类型 |
| BP-CF-2 | Googlebot 伪装 | L3 | cloudflare | 全类型 |
| BP-CF-1 | 源站 IP 直连 | L4 | cloudflare/baidu_waf/chaitin | 全类型 |

### D7.4 性能优化：策略成功率追踪

`BypassStatsTracker` 记录每种策略的历史成功率，动态调整优先级：
- 成功率高的策略 priority 降低（更早尝试）
- 成功率低的策略 priority 升高（更晚尝试）
- 调整公式：`adjusted_priority = base_priority + (1 - success_rate) * penalty_factor`

### 实施清单

| 阶段 | 内容 | 状态 |
|------|------|------|
| D7.1 | `lib/tamper.py`(11 变形函数) + `lib/origin_finder.py` + `lib/waf_bypass.py`(策略库+协调器) + 57 测试 | ✅ |
| D7.2 | `plugins/base.py` + `core/engine.py` + `main.py --bypass-waf` + 4 首批插件改造 + 14 测试 | ✅ |
| D7.3 | 16 全量插件适配 + 15 插件测试 + 25 端到端验收测试 + BypassContext 策略传递修复 | ✅ |
| D7.4 | `BypassStatsTracker` 策略成功率追踪 + 动态优先级 + 报告徽标渲染 + 18 测试 | ✅ |

### 新增文件（8 个）

| 文件 | 说明 |
|------|------|
| `lib/tamper.py` | 11 个 payload 变形纯函数（space2comment/randomcase/between/url_encode 等） |
| `lib/origin_finder.py` | OriginIPFinder（SSL SAN/DNS/headers/子域名 4 路探测） |
| `lib/waf_bypass.py` | 策略基类 + 11 策略类 + Registry + BypassSession + Coordinator + BypassStatsTracker |
| `tests/test_tamper.py` | 变形函数 25 单测 |
| `tests/test_waf_bypass.py` | 策略+协调器 23 单测 |
| `tests/test_origin_finder.py` | 源站探测 9 单测 |
| `tests/test_d7_engine.py` | 引擎集成 14 测试 |
| `tests/test_d7_plugins.py` | 全量插件适配 15 测试 |
| `tests/test_d7_acceptance.py` | 端到端 WAF 绕过验收 25 测试 |
| `tests/test_d7_stats.py` | 策略成功率追踪 18 测试 |

### 修改文件

| 文件 | 改动点 |
|------|--------|
| `plugins/base.py` | 新增 `vuln_type`/`supports_waf_bypass`/`bypass_max_attempts`/`verify_with_bypass` |
| `core/engine.py` | `run()` 加 `waf_bypass_coordinator` 参数 + 4 行绕过钩子 |
| `main.py` | `--bypass-waf auto/on/off` 参数 + Coordinator 构建 + 源站 IP 预探测 |
| `lib/waf_features.py` | 8 WAF 加 `block_signatures` + `recommended_strategies` + `is_waf_blocked()` |
| `config/settings.py` | `WafBypass` 配置类 |
| `core/report.py` | HTML 报告渲染 WAF 绕过徽标（紫色成功/灰色失败） |
| `plugins/ruoyi/*.py` | 16 插件加 `vuln_type` + `supports_waf_bypass` 属性 |

### 测试总量

D7 新增 129 个测试（25 tamper + 23 bypass + 9 origin + 14 engine + 15 plugins + 25 acceptance + 18 stats），全量 317 测试通过，零回归。

---

## D9 · Web API REST 接口（FastAPI + WebSocket + Alpine.js 控制台）— ✅ 已完成

> **阶段定位**：将 CLI 扫描能力通过 FastAPI 暴露为 REST API + WebSocket，提供 Web 控制台（Alpine.js 单页），支持远程提交任务、实时事件推送、报告下载。
> **约束**：CLI 行为零变化（`run_mode` 内部可选调用 orchestrator）、`core/engine.py` 零修改、`core/models.py` 零修改。

### 架构设计

```
CLI (main.py run_mode)     API (api/app.py FastAPI)
       ↓                           ↓
       └──→ ScanOrchestrator ←──────┘
                  ↓
          ┌───────┴───────┐
          ↓               ↓
    ScanEngine.run    TaskRegistry
    (ThreadPool)      (跨线程事件推送)
          ↓               ↓
    ScanResult[]     WebSocket → 浏览器
```

### 四层实现

| 层次 | 技术 | 说明 |
|------|------|------|
| **D9.1 编排器** | `core/orchestrator.py` | ScanRequest/ScanTask/ScanOrchestrator，从 main.py run_mode 抽取，CLI/API 共用 |
| **D9.2 REST API** | FastAPI + 18 端点 | `/api/scan` 提交/查询/取消/结果、`/api/plugins` 列出/查询、`/api/system/health`/version/fingerprint、`/api/report` 元数据/下载（6 格式） |
| **D9.3 WebSocket** | TaskRegistry + asyncio | 跨线程事件投递（`run_coroutine_threadsafe`）、历史事件补播、心跳机制 |
| **D9.4 Web 控制台** | Alpine.js 单页 | 扫描表单、实时结果表格、事件日志、进度条、报告下载、WebSocket 实时推送 |

### 18 个 REST 端点

| 方法 | 路径 | 说明 |
|------|------|------|
| POST | `/api/scan` | 提交扫描任务 |
| GET | `/api/scan` | 列出所有任务 |
| GET | `/api/scan/{task_id}` | 查询任务状态 |
| DELETE | `/api/scan/{task_id}` | 取消任务 |
| GET | `/api/scan/{task_id}/results` | 获取任务结果 |
| GET | `/api/plugins` | 列出所有插件 |
| GET | `/api/plugins/{name}` | 查询单个插件 |
| GET | `/api/system/health` | 健康检查 |
| GET | `/api/system/version` | 版本信息 |
| GET | `/api/system/fingerprint` | 在线指纹探测 |
| GET | `/api/report/{task_id}` | 报告元数据 |
| GET | `/api/report/{task_id}/html` | 下载 HTML 报告 |
| GET | `/api/report/{task_id}/json` | 下载 JSON 报告 |
| GET | `/api/report/{task_id}/csv` | 下载 CSV 报告 |
| GET | `/api/report/{task_id}/pdf` | 下载 PDF 报告 |
| GET | `/api/report/{task_id}/docx` | 下载 Word 报告 |
| GET | `/api/report/{task_id}/xlsx` | 下载 Excel 报告 |
| WS | `/ws/scan/{task_id}` | WebSocket 实时事件推送 |

### WebSocket 事件类型

| 事件类型 | 说明 |
|---------|------|
| `status` | 任务状态变更（pending/running/done/failed/cancelled） |
| `fingerprint` | 指纹识别完成 |
| `waf` | WAF 识别完成 |
| `portscan` | 端口扫描完成 |
| `category_start` | 插件分组开始 |
| `result` | 单个插件产生结果 |
| `progress` | 进度更新（done/total/percent） |
| `report` | 报告生成完成 |
| `complete` | 任务完成（含 duration/result_count） |
| `error` | 任务异常 |

### 实施清单

| 阶段 | 内容 | 状态 |
|------|------|------|
| D9.1 | `core/orchestrator.py`（ScanOrchestrator + ScanRequest + ScanTask）+ 23 测试 | ✅ |
| D9.2 | `api/app.py` + 4 路由模块 + `api/models/schemas.py` + `main.py --serve` + 19 测试 | ✅ |
| D9.3 | `core/task_registry.py` + `api/ws/handler.py` + `api/ws/events.py` + 21 测试 | ✅ |
| D9.4 | `web/index.html`（Alpine.js 单页控制台，26KB） | ✅ |

### 新增文件（18 个）

| 文件 | 说明 |
|------|------|
| `core/orchestrator.py` | ScanOrchestrator + ScanRequest + ScanTask（CLI/API 共用） |
| `core/task_registry.py` | TaskRegistry（任务管理 + 跨线程事件推送桥） |
| `api/__init__.py` | API 包 |
| `api/app.py` | FastAPI 应用工厂（lifespan + 路由注册 + 静态资源） |
| `api/deps.py` | 依赖注入（get_registry/get_orchestrator） |
| `api/models/__init__.py` | 模型包 |
| `api/models/schemas.py` | Pydantic 请求/响应模型 |
| `api/routes/__init__.py` | 路由包 |
| `api/routes/scan.py` | 扫描任务路由（5 端点） |
| `api/routes/report.py` | 报告路由（6 端点） |
| `api/routes/plugin.py` | 插件路由（2 端点） |
| `api/routes/system.py` | 系统路由（3 端点） |
| `api/ws/__init__.py` | WebSocket 包 |
| `api/ws/events.py` | 事件类型常量 |
| `api/ws/handler.py` | WebSocket 连接管理（订阅/补播/心跳） |
| `web/index.html` | Alpine.js 单页控制台 |
| `tests/test_orchestrator.py` | 编排器 23 测试 |
| `tests/test_api_scan.py` | REST API 17 测试 |
| `tests/test_api_ws.py` | TaskRegistry + WebSocket 21 测试 |

### 修改文件

| 文件 | 改动点 |
|------|--------|
| `main.py` | 新增 `--serve`/`--host`/`--port` 参数 + `run_serve_mode()` 函数 + 帮助文本 |
| `requirements.txt` | 新增 `fastapi>=0.100` + `uvicorn[standard]>=0.23`（D9 可选依赖） |

### 测试总量

D9 新增 63 个测试（23 orchestrator + 19 API + 21 WS），全量 380 测试通过，零回归。
